# GAME_DESIGN.md — Keyboard Dodge

## 1. 게임 개요

### 프로젝트명

**Keyboard Dodge**  
표기 후보: **KEY//DODGE**

### 장르

키보드 공간 기억 + 리듬 + 패턴 회피 게임

### 한 줄 설명

화면의 QWERTY 키보드 위에서 실제 알파벳 키를 눌러 즉시 이동하고, 박자에 맞춰 예고되는 위험 셀을 피해 45초를 버틴다.

### 프로토타입 검증 질문

1. 서로 떨어진 키로 즉시 이동해 공격을 피하는 행위 자체가 재미있는가?
2. 공격 예고와 충돌이 일정한 박자에 놓였을 때 더 읽기 쉽고 손맛이 생기는가?
3. 설명 없이도 플레이어가 실제 키보드와 화면 키보드의 공간 관계를 빠르게 학습하는가?

콘텐츠 양, 메타 성장, 온라인 기능보다 이 세 질문에 대한 답을 먼저 얻는다.

## 2. 핵심 플레이 루프

1. 다음 공격의 위험 셀이 화면 키보드에 예고된다.
2. 플레이어는 실제 알파벳 키를 한 번 눌러 안전한 셀로 이동한다.
3. 박자에 맞춰 공격이 충돌한다.
4. 위험 셀에 있으면 HP가 감소하고, 안전하면 콤보가 오른다.
5. 시간이 지날수록 패턴이 넓고 빠르게 조합된다.
6. HP가 남은 채 45초에 도달하면 클리어한다.

한 판은 재시작까지 포함해 짧고 반복 가능해야 한다.

## 3. 플레이 영역

프로토타입은 표준 영문 QWERTY의 알파벳 26키만 사용한다.

```text
Q W E R T Y U I O P
 A S D F G H J K L
  Z X C V B N M
```

화면의 행 오프셋은 실제 키보드의 대략적인 공간감을 표현한다. 숫자, 기능키, 방향키, Space, Tab, Enter, Shift, Ctrl, Alt는 플레이 셀로 사용하지 않는다.

각 셀의 논리 식별자는 `KeyQ`, `KeyW`, …, `KeyM` 형식이다. 화면 라벨은 `Q`, `W`, …, `M`으로 표시한다.

## 4. 플레이어와 입력

### 시작

- 게임 시작 위치는 `KeyF`다.
- 카운트다운 또는 시작 안내가 끝나기 전 입력은 위치를 바꾸지 않는다.
- 재시작하면 위치, HP, 콤보, 타이머, 무적 상태를 모두 초기화한다.

### 이동 규칙

- `keydown`의 `KeyboardEvent.code`가 `KeyA`~`KeyZ`일 때만 유효하다.
- `event.repeat === true`인 자동 반복 입력은 무시한다.
- 마지막 유효 키 입력이 곧 현재 위치다.
- 키를 계속 누르고 있을 필요가 없다.
- 이동 시간, 경로, 거리, 인접 제한은 없다.

예를 들어 현재 위치가 F일 때 J를 누르면 즉시 J로, 이어서 U를 누르면 즉시 U로 이동한다. F에서 P처럼 멀리 떨어진 이동도 허용한다.

### 입력 예외

- 한글 IME 등 현재 입력 언어와 무관하게 물리 QWERTY 위치를 유지한다.
- 게임이 일시정지됐거나 종료됐을 때 알파벳 입력은 이동으로 처리하지 않는다.
- 입력 가능한 폼 요소를 향후 추가한다면 그 요소에 포커스된 동안 게임 입력을 받지 않는다.
- 창이 포커스를 잃거나 문서가 숨겨지면 자동 일시정지해 보이지 않는 동안의 피해를 막는다.

## 5. 공격과 판정

### 공격 생명주기

각 공격 이벤트는 다음 세 단계를 갖는다.

1. **예고(Telegraph):** 위험 셀을 미리 표시하고 박자 펄스나 카운트로 충돌 시점을 알린다.
2. **충돌(Impact):** 예정된 한 시점에 플레이어의 현재 셀을 검사한다.
3. **해제(Release):** 위험 표시를 빠르게 걷고 다음 패턴을 읽을 여백을 준다.

판정은 충돌 순간 한 번만 수행한다. 애니메이션 프레임 동안 계속 충돌시키지 않는다.

### 셀 기반 충돌

플레이어와 공격은 픽셀 Hitbox를 갖지 않는다. 공격 대상 집합에 현재 셀 식별자가 포함되는지만 검사한다.

```text
위험 셀: KeyD, KeyF, KeyG
현재 위치: KeyF
결과: HIT
```

현재 위치가 `KeyH`라면 `DODGE`다. 시각 애니메이션이 셀 경계를 넘더라도 논리 판정은 변하지 않는다.

### 공정성 규칙

- 모든 공격은 예고 시작부터 충돌까지 반응 가능한 시간을 제공한다.
- 어떤 패턴도 26개 셀 전체를 동시에 막지 않는다.
- 패턴 생성 후 최소 하나의 안전 셀이 있는지 검사한다.
- 겹치는 공격은 한눈에 하나의 충돌 시점으로 읽히거나, 충돌 시점이 명확히 분리돼야 한다.
- 피격 연출 때문에 다음 공격의 예고가 가려져서는 안 된다.

## 6. HP, 무적, 종료

- 최대 HP는 **5**다.
- 충돌 순간 위험 셀에 있으면 HP가 1 감소한다.
- 실제 피해가 적용된 시점부터 **500ms** 동안 무적이다.
- 무적 중 충돌한 공격은 추가 HP를 깎지 않는다.
- 무적 여부와 관계없이 공격 자체는 정해진 시각 효과와 사운드를 재생할 수 있다.
- HP가 0이 되는 즉시 게임 오버 상태로 전환하고 게임 시간을 정지한다.
- HP가 1 이상인 상태에서 플레이 시간이 **45.0초**에 도달하면 클리어한다.
- 같은 업데이트에서 HP 0과 45초 도달이 겹치면 예정된 공격 충돌을 먼저 판정하고, 살아남았을 때만 클리어한다.

## 7. 리듬 시스템

### 핵심 원칙

리듬은 공격을 예측하고 움직임에 손맛을 주는 시간 구조다. 생존을 위한 별도의 정확도 시험이 아니다.

- 공격 예고, 펄스, 충돌, 효과음은 공통 박자 시계에서 파생한다.
- 플레이어는 충돌 전이라면 어느 시점에 이동해도 된다.
- `Perfect`, `Good`, `Miss` 같은 입력 타이밍 평가가 있더라도 피격 여부나 HP를 바꾸지 않는다.
- 생존 판정은 오직 충돌 순간의 현재 셀로 결정한다.

### 기본 템포

- 프로토타입 기본값은 **120 BPM**이며 한 박은 500ms다.
- BPM은 조정 가능한 단일 설정값으로 둔다.
- 난이도는 BPM을 무리하게 올리기보다 대상 셀 수, 공간 패턴, 연속 충돌 간격, 예고 길이의 조합으로 만든다.
- 오디오 재생 위치가 게임 판정의 유일한 시계가 되어서는 안 된다. 렌더링, 오디오, 판정은 동일한 논리 타임라인을 공유하되 일시정지와 오디오 잠금 해제를 안전하게 처리한다.

## 8. 패턴 언어와 난이도

초기 패턴은 키보드 공간을 눈으로 읽을 수 있는 단순한 도형으로 구성한다.

### 패턴 후보

- **단일 셀:** 특정 키 하나를 공격한다. 튜토리얼과 첫 박에 사용한다.
- **가로 구간:** 한 행의 연속된 여러 키를 공격한다.
- **세로/대각선 띠:** QWERTY 오프셋을 반영한 셀 묶음을 공격한다.
- **좌/우 반면:** 키보드 한쪽을 막아 반대쪽 이동을 유도한다.
- **링/구멍:** 넓은 영역을 막되 눈에 띄는 안전 셀 또는 안전 구역을 남긴다.
- **교대:** 좌우나 위아래 패턴을 연속 박에 번갈아 충돌시킨다.

무작위 셀만 흩뿌리는 패턴은 공간적으로 읽기 어렵기 때문에 보조적으로만 사용한다.

### 45초 난이도 곡선

| 구간 | 의도 | 패턴 구성 | 예고 기준 |
| --- | --- | --- | --- |
| 0~15초 | 규칙 학습 | 단일 셀, 짧은 가로 구간, 느린 교대 | 대체로 2박 |
| 15~30초 | 공간 이동 유도 | 행/열/반면 패턴, 2개 패턴 연속 | 1~2박 |
| 30~45초 | 숙련 확인 | 넓은 패턴, 빠른 교대, 복합 시퀀스 | 최소 1박 |

정확한 셀 수와 패턴 가중치는 플레이테스트 항목이다. 난이도는 경과 시간만으로 결정하며 콤보가 높다고 공격을 더 어렵게 만들지 않는다.

## 9. 콤보와 피드백

- 공격 충돌을 피해 없이 통과할 때 콤보가 1 증가한다.
- 실제 피해를 받으면 콤보가 0으로 초기화된다.
- 무적 중 위험 셀에 있던 경우는 추가 피해는 없지만 성공적인 회피도 아니므로 콤보가 증가하지 않는다.
- 콤보는 숙련 피드백과 연출 강도에 사용한다.
- 프로토타입의 승패는 점수나 콤보가 아니라 45초 생존 여부로 결정한다.
- 최고 콤보는 결과 화면에 표시해 재도전 동기를 준다.

## 10. 게임 상태

게임은 최소 다음 상태를 명시적으로 가진다.

```text
READY → PLAYING ⇄ PAUSED
             ├→ CLEARED
             └→ GAME_OVER
```

- **READY:** 규칙과 시작 방법을 보여준다. 첫 상호작용으로 오디오를 준비할 수 있다.
- **PLAYING:** 시간, 입력, 공격, 콤보를 갱신한다.
- **PAUSED:** 모든 논리 시간을 동결하고 재개 안내를 보여준다.
- **CLEARED:** 45초 생존 결과와 재시작을 보여준다.
- **GAME_OVER:** 생존 시간과 최고 콤보, 재시작을 보여준다.

`Escape` 또는 화면 버튼으로 일시정지/재개할 수 있다. 탭 비활성화에 의한 자동 일시정지 뒤에는 사용자가 명시적으로 재개한다.

## 11. 화면 구성

### 상단 HUD

- 남은 시간 또는 45초 진행도
- HP 5칸
- 현재 콤보와 최고 콤보
- 사운드 음소거 버튼
- 일시정지 버튼

### 중앙 플레이 영역

- 3행 QWERTY 키보드
- 현재 플레이어 셀의 명확한 표시
- 위험 예고, 충돌, 피격, 안전 회피 상태
- 다음 충돌까지의 박자 펄스

### 오버레이

- 시작 안내
- 일시정지
- 클리어
- 게임 오버
- 재시작 동작

화면은 한눈에 키보드 전체와 HUD가 함께 들어오는 데스크톱 가로 구성을 우선한다.

## 12. 시각·사운드 원칙

### 시각

- 현재 위치, 예고 위험, 충돌 위험, 무적 상태는 서로 다른 실루엣이나 테두리를 사용한다.
- 예고는 충분히 밝지만 충돌보다 덜 강해야 한다.
- 충돌 순간에는 짧은 스케일/글로우 변화로 박자를 강조한다.
- 피격 플래시와 화면 흔들림은 짧게 제한한다.
- 색각과 모션 민감도를 고려해 색 이외의 단서 및 reduced-motion 대안을 제공한다.

### 사운드

- 예고 틱, 충돌 임팩트, 이동, 성공 회피, 피격, 시작/종료 큐를 구분한다.
- 배경 리듬이 없어도 핵심 박자를 알 수 있어야 한다.
- 브라우저 자동 재생 정책 때문에 첫 클릭 또는 키 입력 뒤 오디오를 활성화한다.
- 음소거 상태에서도 모든 타이밍 정보가 시각적으로 전달돼야 한다.

## 13. 프로토타입 범위

### 반드시 포함

- QWERTY 26셀 렌더링
- `KeyboardEvent.code` 기반 즉시 이동
- F 시작 위치
- 박자 기반 공격 예고와 셀 충돌
- HP 5, 500ms 무적
- 45초 타이머와 시간 기반 난이도 상승
- 콤보와 최고 콤보
- READY/PLAYING/PAUSED/CLEARED/GAME_OVER 흐름
- 기본 효과음과 음소거
- 재시작

### 이번 프로토타입에서 제외

- 회원가입, 클라우드 저장, 온라인 순위표
- 스토리, 캐릭터 선택, 성장 및 상점
- 곡 선택과 외부 음원 동기화
- 모바일 터치용 별도 조작 체계
- 픽셀 단위 이동과 물리 충돌
- 숫자키·특수키·방향키를 이용한 능력
- 맵 편집기와 사용자 제작 패턴

## 14. 플레이테스트 성공 기준

첫 내부 플레이테스트에서 다음을 관찰한다.

- 10초 안에 “누른 키로 이동한다”는 규칙을 이해하는가.
- 공격 예고만 보고 충돌 시점을 예측할 수 있는가.
- 실패 원인을 자신의 위치 선택으로 납득하는가.
- 45초가 지루하게 길거나 학습 전에 끝나지 않는가.
- 다시 시도할 때 더 나아질 수 있다는 느낌이 드는가.
- 한글 입력 상태, 낮은 프레임률, 탭 전환에서도 판정이 일관적인가.

핵심 루프가 재미없다면 콘텐츠를 늘리기 전에 이동 방식, 예고 길이, 패턴 가독성, 한 판 길이를 먼저 조정한다.

