# GAME_DESIGN.md — Keyboard Dodge

## 1. 게임 개요

### 프로젝트명

**Keyboard Dodge**  
표기 후보: **KEY//DODGE**

### 장르

키보드 공간 기억 + 리듬 + 패턴 회피 게임

### 한 줄 설명

완전히 분리된 타이틀에서 여섯 스테이지 중 하나를 고르고, 실제 알파벳 키로 QWERTY 셀을 이동하며 템포와 활성 영역이 변주되는 공격 시퀀스를 끝까지 돌파한다.

### 프로토타입 검증 질문

1. 서로 떨어진 키로 즉시 이동해 공격을 피하는 행위 자체가 재미있는가?
2. 공격 예고와 충돌이 일정한 박자에 놓였을 때 더 읽기 쉽고 손맛이 생기는가?
3. 설명 없이도 플레이어가 실제 키보드와 화면 키보드의 공간 관계를 빠르게 학습하는가?

콘텐츠 양, 메타 성장, 온라인 기능보다 이 세 질문에 대한 답을 먼저 얻는다.

## 2. 핵심 플레이 루프

1. 완전히 분리된 타이틀 화면에서 `Space`, `Enter`, 또는 시작 버튼 클릭으로 스테이지 선택에 들어간다.
2. 초기 강조가 없는 STAGE_SELECT에서 Q/W/E/R/T/Y나 카드 클릭으로 대상을 먼저 강조한 뒤, 같은 입력의 두 번째 조작 또는 `Enter`로 확정한다.
3. 확정한 스테이지의 오프닝 BPM 한 박 간격으로 `3 → 2 → 1` 카운트다운을 진행한다.
4. 현재 템포 섹션과 활성 키 존을 확인하고, 다음 웨이브의 위험 셀·존 변경 또는 희귀 회복 셀 예고를 읽는다.
5. 플레이어는 현재 존 안의 실제 알파벳 키를 눌러 안전한 셀 또는 회복 목표 셀로 즉시 이동한다.
6. 감속·가속·러시 구간의 현재 BPM에 맞춰 웨이브가 판정되고, 패턴에 따라 키 존이 확장되거나 축소된다.
7. 위험 셀에 있으면 HP가 감소하고, 안전하게 피하면 콤보가 오른다. 회복 조건을 만족하면 HP가 1 회복된다.
8. HP가 남은 채 선택한 스테이지의 마지막 웨이브를 통과하면 클리어한다.

한 판은 재시작까지 포함해 짧고 반복 가능해야 한다.

## 3. 플레이 영역

프로토타입의 전체 플레이 공간은 표준 영문 QWERTY의 알파벳 26키다. 실제로 이동하고 판정하는 활성 키는 스테이지마다 한 번 고정되지 않고 현재 웨이브의 **활성 키 존**에 따라 확장·축소된다.

```text
Q W E R T Y U I O P
 A S D F G H J K L
  Z X C V B N M
```

화면의 행 오프셋은 실제 키보드의 대략적인 공간감을 표현한다. 숫자, 기능키, 방향키, Space, Tab, Enter, Shift, Ctrl, Alt는 플레이 셀로 사용하지 않는다.

각 셀의 논리 식별자는 `KeyQ`, `KeyW`, …, `KeyM` 형식이다. 화면 라벨은 `Q`, `W`, …, `M`으로 표시한다.

### 활성 키 존

존은 26키 중 현재 이동·공격·안전 판정에 참여하는 키 집합이다. 구현은 아래 역할을 만족하는 재사용 가능한 존 카탈로그에서 패턴별 존을 선택한다.

- **HOME:** 홈로우 중심의 작은 입문 존
- **CORE:** F 주변과 인접 행을 포함하는 중간 존
- **LEFT / RIGHT:** 한쪽 공간 읽기를 강조하되 반드시 F를 포함하는 편향 존
- **WIDE:** 가장자리 일부만 잠긴 넓은 존
- **FULL:** 알파벳 26키 전체

모든 존은 공통 기준점인 `KeyF`를 포함한다. 존 집합, 화면 잠금 표시, 입력 필터, 공격 대상과 안전 셀 계산은 하나의 현재 존 값에서 파생한다.

### 스테이지별 길이와 템포 설계

| 단축키 | 스테이지 | 웨이브 | 오프닝 BPM | BPM 범위 | 활성 키 범위 | 경험 목표 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| Q | STAGE 01 | 12 | 92 | 82–110 | 5–11 | 이동, 존 예고와 템포 변화 학습 |
| W | STAGE 02 | 14 | 100 | 88–122 | 8–15 | 행을 넘는 이동과 재집결 |
| E | STAGE 03 | 16 | 108 | 94–132 | 10–19 | 첫 `ONLY SAFE`와 넓은 탐색 |
| R | STAGE 04 | 18 | 116 | 102–142 | 12–22 | 빠른 방향 전환과 존 축소 대응 |
| T | STAGE 05 | 21 | 124 | 108–150 | 15–25 | 긴장 완급과 고밀도 복합 이동 |
| Y | STAGE 06 | 24 | 132 | 116–156 | 18–26 | 전체 시스템 숙련 시험 |

- 여섯 스테이지는 처음부터 모두 선택 가능하며 해금 조건을 두지 않는다.
- 각 스테이지는 오프닝 BPM 하나는 갖지만 플레이 전체를 같은 BPM으로 진행하지 않는다. 선택 화면에는 템포 범위와 주요 변화 경로를 보여준다.
- 모든 스테이지는 `OPENING → EXPANSION/SPEED-UP → CONTRACTION/SLOWDOWN → LARGER/FASTER FINALE`의 네 구간으로 구성한다. 구간 안의 실제 BPM과 존은 표의 범위에서 웨이브별로 결정된다.
- 템포 구간, 웨이브 수, 존 순서, 화면 표시, 입력 필터와 공격 대상 생성은 하나의 공통 스테이지 시퀀스 설정에서 파생한다.
- 비활성 키는 잠긴 셀로 구분하거나 숨길 수 있지만 공격·이동·안전 셀 계산에는 포함하지 않는다.
- 모든 존에 `KeyF`가 포함되므로 시작 위치와 존 축소 시 재집결 기준은 변하지 않는다.

## 4. 플레이어와 입력

### 시작

- 앱을 열면 게임 HUD나 스테이지 카드가 보이지 않는 별도 `TITLE` 화면을 표시한다.
- `TITLE`에서는 `Space`, `Enter`, 또는 시작 버튼 클릭만 `STAGE_SELECT` 진입으로 처리하며 아직 게임을 시작하지 않는다.
- `STAGE_SELECT`에 처음 진입하거나 다시 돌아왔을 때 강조된 카드는 없다.
- 물리 `KeyQ`/`KeyW`/`KeyE`/`KeyR`/`KeyT`/`KeyY`의 첫 입력은 각각 STAGE 01~06 카드를 강조하고 포커스만 옮긴다. 같은 키를 다시 입력하면 해당 스테이지를 확정한다.
- 카드의 첫 클릭은 그 카드의 강조와 포커스만 설정한다. 이미 강조된 같은 카드를 다시 클릭하면 확정한다. 다른 카드를 클릭하면 새 카드로 강조만 옮긴다.
- 화살표 키는 카드 강조를 만들거나 이동하고, `Enter`는 현재 강조된 카드가 있을 때만 확정한다. 강조가 없는 상태의 `Enter`는 아무 스테이지도 시작하지 않는다.
- 스테이지 확정은 즉시 플레이를 시작하지 않고 `COUNTDOWN`으로 전환한다.
- 게임 시작 위치는 `KeyF`다.
- `PLAYING`에 진입하기 전 입력은 플레이어 위치를 바꾸지 않는다.
- 재시작하면 위치, HP, 콤보, 웨이브 진행도, 무적 및 회복 입력 상태를 모두 초기화한다.

### 이동 규칙

- `keydown`의 `KeyboardEvent.code`가 `KeyA`~`KeyZ`이고 현재 웨이브의 활성 키 존에 포함될 때만 이동 입력으로 유효하다.
- `event.repeat === true`인 자동 반복 입력은 무시한다.
- 마지막 유효 키 입력이 곧 현재 위치다.
- 키를 계속 누르고 있을 필요가 없다.
- 이동 시간, 경로, 거리, 인접 제한은 없다.
- 현재 존 밖의 알파벳 키를 눌러도 현재 위치, 콤보, 회복 입력 상태가 바뀌지 않는다.

예를 들어 현재 위치가 F일 때 J가 현재 존에 포함돼 있다면 즉시 J로 이동한다. U나 P가 활성 상태라면 F에서처럼 멀리 떨어진 셀로도 즉시 이동할 수 있다. 이후 존이 축소돼 그 키가 잠기면 다음 유효 입력에는 사용할 수 없다.

### 입력 예외

- 한글 IME 등 현재 입력 언어와 무관하게 물리 QWERTY 위치를 유지한다.
- TITLE, STAGE_SELECT, COUNTDOWN, 일시정지, 종료 상태의 알파벳 입력은 이동으로 처리하지 않는다. STAGE_SELECT의 `KeyQ`/`KeyW`/`KeyE`/`KeyR`/`KeyT`/`KeyY`는 이동이 아닌 강조·확정 입력이다.
- 입력 가능한 폼 요소를 향후 추가한다면 그 요소에 포커스된 동안 게임 입력을 받지 않는다.
- 창이 포커스를 잃거나 문서가 숨겨지면 자동 일시정지해 보이지 않는 동안의 피해를 막는다.
- COUNTDOWN에서 `Escape`를 누르거나 창 포커스·문서 가시성을 잃으면 카운트다운을 취소하고 STAGE_SELECT로 복귀한다. 예정된 카운트다운 콜백과 게임 시작 예약은 모두 무효화한다.

## 5. 공격과 판정

### 공격 생명주기

각 공격 이벤트는 다음 세 단계를 갖는다.

1. **예고(Telegraph):** 위험 셀을 미리 표시하고 박자 펄스나 카운트로 충돌 시점을 알린다.
2. **충돌(Impact):** 예정된 한 시점에 플레이어의 현재 셀을 검사한다.
3. **해제(Release):** 위험 표시를 빠르게 걷고 다음 패턴을 읽을 여백을 준다.

판정은 충돌 순간 한 번만 수행한다. 애니메이션 프레임 동안 계속 충돌시키지 않는다.

### 셀 기반 충돌

플레이어와 공격은 픽셀 Hitbox를 갖지 않는다. 공격 대상 집합에 현재 셀 식별자가 포함되는지만 검사한다.

```text
위험 셀: KeyD, KeyF, KeyG
현재 위치: KeyF
결과: HIT
```

현재 위치가 `KeyH`라면 `DODGE`다. 시각 애니메이션이 셀 경계를 넘더라도 논리 판정은 변하지 않는다.

### 공정성 규칙

- 모든 공격은 예고 시작부터 충돌까지 반응 가능한 시간을 제공한다.
- 공격은 현재 활성 키 존 안의 셀만 대상으로 삼으며 존 밖의 잠긴 키는 공격 대상도 안전 셀도 아니다.
- 어떤 공격 패턴도 현재 존의 활성 키 전체를 동시에 막지 않는다.
- 시퀀스를 로드하거나 검증할 때 각 공격에 최소 하나의 현재 존 안전 셀이 있는지 검사한다.
- 겹치는 공격은 한눈에 하나의 충돌 시점으로 읽히거나, 충돌 시점이 명확히 분리돼야 한다.
- 피격 연출 때문에 다음 공격의 예고가 가려져서는 안 된다.

### 존 변경 순서

- **확장:** 새 키를 활성화하고 `ZONE EXPAND`와 새 활성 키 수를 최소 한 박 표시한다. 기존 플레이어 위치는 유지한다.
- **축소:** 새 존을 계산한 뒤 플레이어가 존 밖이면 공격·회복·콤보 판정 없이 먼저 `KeyF`로 재배치한다.
- 재배치가 필요했는지와 무관하게 `ZONE CONTRACT` 및 새 활성 키 수를 **한 박 전체** 예고한다. 이 예고가 끝나기 전에는 새 공격을 충돌시키지 않는다.
- 존 변경 웨이브는 그 자체로 공격 회피나 회복 웨이브로 세지 않는다. 다음 공격부터 이동 필터, 대상 생성과 안전 셀 계산이 새 존을 사용한다.
- 일시정지 중에는 존 변경 예고의 남은 시간도 동결한다.

### 희귀 회복 웨이브

- STAGE 03~06의 설계된 시퀀스에만 제한적으로 배치하고 각 해당 스테이지에 1회만 둔다.
- 한 스테이지에서 연속 출현시키지 않고 `ONLY SAFE` 직전·직후에도 배치하지 않는다.
- 예고 중 하나의 활성 키를 파랑 또는 초록 계열과 `HEAL` 라벨로 표시한다.
- 플레이어가 예고 구간 중 그 키를 실제로 한 번 입력하고, 판정 순간에도 같은 셀에 있으면 HP를 1 회복한다.
- HP는 최대 5를 넘지 않는다. 이미 HP 5인 성공에는 최대치 피드백만 표시한다.
- 입력 이력과 판정 순간 위치 중 하나라도 조건을 만족하지 못하면 회복하지 않지만, 실패 피해도 없다.
- 반복 `keydown`과 현재 존 밖의 키 입력은 회복 입력으로 인정하지 않는다.
- 회복 웨이브는 공격 회피로 세지 않으며 콤보를 증가시키거나 초기화하지 않는다.

## 6. HP, 무적, 종료

- 최대 HP는 **5**다.
- 충돌 순간 위험 셀에 있으면 HP가 1 감소한다.
- 실제 피해가 적용된 시점부터 **500ms** 동안 무적이다.
- 무적 중 충돌한 공격은 추가 HP를 깎지 않는다.
- 무적 여부와 관계없이 공격 자체는 정해진 시각 효과와 사운드를 재생할 수 있다.
- HP가 0이 되는 즉시 게임 오버 상태로 전환하고 게임 시간을 정지한다.
- HP가 1 이상인 상태에서 선택한 스테이지의 마지막 웨이브 판정을 마치면 클리어한다.
- 마지막 웨이브에서 HP 0과 시퀀스 완료가 겹치면 웨이브 판정을 먼저 적용하고, 살아남았을 때만 클리어한다.
- 경과 시간은 결과와 디버깅에 표시할 수 있지만 승리 조건으로 사용하지 않는다.

## 7. 리듬 시스템

### 핵심 원칙

리듬은 공격을 예측하고 움직임에 손맛을 주는 시간 구조다. 생존을 위한 별도의 정확도 시험이 아니다.

- 공격 예고, 펄스, 충돌, 효과음은 공통 박자 시계에서 파생한다.
- 플레이어는 충돌 전이라면 어느 시점에 이동해도 된다.
- `Perfect`, `Good`, `Miss` 같은 입력 타이밍 평가가 있더라도 피격 여부나 HP를 바꾸지 않는다.
- 생존 판정은 오직 충돌 순간의 현재 셀로 결정한다.

### 스테이지 내부 템포 섹션

한 스테이지를 하나의 BPM으로 고정하지 않는다. 각 고정 시퀀스는 다음 템포 섹션을 조합한다.

- **OPENING:** 카운트다운과 첫 패턴이 사용하는 기준 템포·초기 존이다.
- **EXPANSION / SPEED-UP:** 존을 넓히면서 여러 박 또는 웨이브에 걸쳐 BPM을 상승시킨다. 즉시 점프하지 않고 설정된 시작·끝 BPM 사이를 결정적으로 보간한다.
- **CONTRACTION / SLOWDOWN:** 존을 줄이고 BPM을 낮춰 재집결과 다음 변화 준비에 여백을 준다.
- **LARGER / FASTER FINALE:** 앞 구간보다 큰 존과 빠른 BPM으로 끝내는 러시다. 반응 가능 시간과 안전 셀 규칙은 그대로 지킨다.

- 각 웨이브의 현재 BPM, 한 박 길이, 예고 시간과 다음 판정 시점은 스테이지 템포 맵에서 계산한다.
- 템포 변화는 미리 정한 고정 시퀀스이므로 같은 스테이지를 재시도하면 같은 웨이브에서 같은 BPM 변화가 재생된다.
- HUD는 정적인 스테이지 BPM 대신 **현재 BPM**과 `SLOWDOWN`, `ACCEL`, `RUSH` 같은 현재 섹션명을 실시간으로 표시한다.
- 상위 스테이지일수록 더 높은 최대 BPM, 더 큰 변화 폭, 더 잦은 방향 전환을 사용할 수 있다. 단, 예고 박 수와 존 크기를 함께 조정해 반응 가능 시간을 보장한다.
- 오디오 재생 위치가 게임 판정의 유일한 시계가 되어서는 안 된다. 렌더링, 오디오, 판정은 동일한 논리 타임라인을 공유하되 일시정지와 오디오 잠금 해제를 안전하게 처리한다.

### 3박 시작 카운트다운

- 스테이지 선택 확정, 결과 화면의 RETRY, NEXT STAGE는 모두 `COUNTDOWN`으로 진입한다.
- `COUNTDOWN` 진입 즉시 `3`을 표시하고, 선택한 스테이지의 **오프닝 BPM** 한 박 뒤 `2`, 다시 한 박 뒤 `1`을 표시한 뒤 마지막 한 박이 지나면 `PLAYING`을 시작한다.
- 카운트다운 숫자, 펄스와 효과음은 오프닝 BPM에서 파생한다. 플레이 도중의 감속·가속·러시는 카운트다운을 소급해 바꾸지 않는다.
- COUNTDOWN 중에는 이동, 웨이브, 공격, 플레이 타이머, 콤보와 무적 시간이 진행되지 않는다.
- 카운트다운 일정도 단조 시계를 사용하고, 취소되거나 화면을 벗어난 이전 일정은 이후 PLAYING을 시작시킬 수 없다.

## 8. 패턴 언어와 난이도

초기 패턴은 키보드 공간을 눈으로 읽을 수 있는 단순한 도형으로 구성한다.

### 패턴 후보

- **단일 셀:** 특정 키 하나를 공격한다. 튜토리얼과 첫 박에 사용한다.
- **가로 구간:** 한 행의 연속된 여러 키를 공격한다.
- **세로/대각선 띠:** QWERTY 오프셋을 반영한 셀 묶음을 공격한다.
- **좌/우 반면:** 키보드 한쪽을 막아 반대쪽 이동을 유도한다.
- **링/구멍:** 넓은 영역을 막되 눈에 띄는 안전 셀 또는 안전 구역을 남긴다.
- **교대:** 좌우나 위아래 패턴을 연속 박에 번갈아 충돌시킨다.
- **하나만 안전:** 상위 스테이지에서 현재 존의 활성 키 중 정확히 한 키만 남기고 나머지를 모두 공격한다. 안전 키에는 `ONLY SAFE` 라벨을 표시한다.
- **회복:** STAGE 03~06에서 각 1회, 현재 존의 한 키를 `HEAL` 목표로 지정하고 입력 이력과 판정 위치를 함께 검사한다.
- **존 확장:** 작은 존에서 더 넓은 존으로 전환해 다음 패턴의 탐색 범위를 넓힌다.
- **존 축소:** 더 작은 존으로 전환하고 필요하면 F로 재집결시킨 뒤 한 박 전체를 경고한다.

무작위 셀만 흩뿌리는 패턴은 공간적으로 읽기 어렵기 때문에 보조적으로만 사용한다.

### 스테이지별 고정 시퀀스

| 스테이지 | 시퀀스 구성 | 특수 패턴 | 템포·존 가독성 기준 |
| --- | --- | --- | --- |
| STAGE 01 · 12웨이브 | 단일 셀·짧은 구간·느린 교대 | 첫 확장·축소 튜토리얼 | 완만한 감속/가속, 존 변화 전 한 박 경고 |
| STAGE 02 · 14웨이브 | 행간 이동·반면·복합 교대 | 반복 존 전환 | 속도와 범위를 한 번에 바꾸지 않는 입문 변주 |
| STAGE 03 · 16웨이브 | 넓은 띠·교대 연속 | 첫 `ONLY SAFE`, 회복 1회 | 감속 뒤 첫 러시, 작은 존과 넓은 존 대비 |
| STAGE 04 · 18웨이브 | 편향 공간·반전 연속 | `ONLY SAFE`, 회복 1회, 급축소 | 다단 템포 변화와 좌우 존 전환 |
| STAGE 05 · 21웨이브 | 넓은 복합 패턴·연속 러시 | 회복 1회, `ONLY SAFE`, 반복 축소 | 깊은 감속 뒤 긴 가속, FULL 근접 존 고밀도 |
| STAGE 06 · 24웨이브 | 전체 패턴 언어의 결합 | 회복 1회 및 모든 특수 패턴 | 템포·존 방향이 여러 차례 변하는 최종 시험 |

- 각 스테이지는 재시도할 때 동일한 순서와 대상으로 재생되는 고정 시퀀스를 가진다.
- 시퀀스 안에서도 초반은 단순하고 후반은 복잡하게 배열해 학습 곡선을 만든다.
- 스테이지가 높을수록 템포 범위, 패턴 복잡도와 존 변화 빈도가 증가하지만 키 수와 BPM은 스테이지 안에서도 오르내린다.
- 현재 HP나 콤보에 따라 공격을 동적으로 바꾸지 않는다.
- 모든 고정 웨이브는 템포 섹션, 현재 존, 대상, 예고 시점, 판정 시점, 활성 안전 셀 존재 여부를 데이터 검증한다.

## 9. 콤보와 피드백

- 공격 충돌을 피해 없이 통과할 때 콤보가 1 증가한다.
- 실제 피해를 받으면 콤보가 0으로 초기화된다.
- 무적 중 위험 셀에 있던 경우는 추가 피해는 없지만 성공적인 회피도 아니므로 콤보가 증가하지 않는다.
- 콤보는 숙련 피드백과 연출 강도에 사용한다.
- 프로토타입의 승패는 점수나 콤보가 아니라 선택한 스테이지의 고정 시퀀스 완주 여부로 결정한다.
- 최고 콤보는 결과 화면에 표시해 재도전 동기를 준다.

## 10. 게임 상태

게임은 최소 다음 상태를 명시적으로 가진다.

```text
TITLE → STAGE_SELECT → COUNTDOWN → PLAYING ⇄ PAUSED
            ↑              │             ├→ CLEARED
            └── ESC/BLUR ──┘             └→ GAME_OVER
```

- **TITLE:** 게임 로고와 시작 동작만 강조하는 완전한 별도 화면이다. `Space`, `Enter`, 클릭으로 STAGE_SELECT에 진입한다.
- **STAGE_SELECT:** 처음부터 열려 있는 STAGE 01~06의 웨이브 수, 오프닝/범위 BPM과 활성 키 범위를 비교한다. 초기 강조는 없고 Q/W/E/R/T/Y와 클릭은 강조 후 같은 입력으로 확정하며, 화살표+`Enter`는 탐색과 확정을 나눈다.
- **COUNTDOWN:** 확정된 스테이지의 오프닝 BPM으로 3→2→1을 한 박씩 표시한다. Escape나 포커스 상실 시 STAGE_SELECT로 복귀한다.
- **PLAYING:** 논리 시간, 입력, 현재 웨이브, 공격·회복 판정, 콤보를 갱신한다.
- **PAUSED:** 모든 논리 시간을 동결하고 재개 안내를 보여준다.
- **CLEARED:** 선택한 스테이지와 완주 결과, 최고 콤보를 보여준다.
- **GAME_OVER:** 도달 웨이브와 최고 콤보를 보여준다.

`Escape` 또는 화면 버튼으로 일시정지/재개할 수 있다. 탭 비활성화에 의한 자동 일시정지 뒤에는 사용자가 명시적으로 재개한다.

### 결과 화면 동선

- **RETRY:** 같은 스테이지를 F, HP 5, 0웨이브로 초기화한 뒤 해당 BPM의 COUNTDOWN을 시작한다.
- **STAGE SELECT:** STAGE_SELECT로 돌아가 다른 스테이지를 선택한다.
- **NEXT STAGE:** STAGE 01~05를 클리어했을 때만 다음 스테이지의 오프닝 BPM COUNTDOWN을 시작한다.
- GAME_OVER에는 NEXT STAGE를 제공하지 않고, STAGE 06 클리어에서는 다음 스테이지 동작을 숨기거나 비활성화한다.
- 결과 화면의 `Space`/`Enter`는 강조된 기본 동작을 실행한다. STAGE 01~05 클리어에서는 NEXT STAGE, GAME_OVER 또는 STAGE 06 클리어에서는 RETRY가 기본 동작이며 어느 경우든 PLAYING 직전 COUNTDOWN을 거친다.

## 11. 화면 구성

### 상단 HUD

- 선택 스테이지, **현재 BPM**, 템포 섹션과 현재/전체 웨이브 진행도
- 현재 활성 키 수와 `ZONE EXPAND`/`ZONE CONTRACT` 변경 예고
- HP 5칸
- 현재 콤보와 최고 콤보
- 사운드 음소거 버튼
- 일시정지 버튼

### 중앙 플레이 영역

- 3행 QWERTY 키보드
- 현재 패턴 존의 활성·비활성 키 구분
- 현재 플레이어 셀의 명확한 표시
- 위험 예고, 하나만 안전, 회복 목표, 충돌, 피격, 안전 회피 상태
- 다음 충돌까지의 박자 펄스

### 별도 화면과 오버레이

- HUD와 스테이지 카드가 보이지 않는 TITLE
- 초기 강조가 없는 STAGE 01~06 카드와 “한 번 강조, 다시 확정” 안내가 있는 STAGE_SELECT
- 선택한 스테이지 코드, 오프닝 BPM, 큰 3→2→1 숫자와 박자 펄스를 표시하는 COUNTDOWN
- 일시정지
- 클리어
- 게임 오버
- 같은 스테이지 재시작, 스테이지 선택 복귀, 조건부 다음 스테이지

화면은 한눈에 키보드 전체와 HUD가 함께 들어오는 데스크톱 가로 구성을 우선한다.

## 12. 시각·사운드 원칙

### 시각

- 현재 위치, 예고 위험, 충돌 위험, 무적 상태는 서로 다른 실루엣이나 테두리를 사용한다.
- 현재 위치가 공격 대상이면 D-016의 빨간 외곽 링, 화면 테두리, 상단 경고와 `TARGET / MOVE!` 라벨을 함께 사용한다.
- 하나만 안전한 패턴의 안전 키에는 `ONLY SAFE`, 회복 키에는 `HEAL` 라벨을 표시한다.
- 현재 BPM과 템포 섹션명은 항상 함께 표시하고, 가속·감속은 숫자 변화 외에 방향 표시를 사용한다.
- 존 변경은 잠금 셀 변화만으로 전달하지 않고 `ZONE EXPAND`/`ZONE CONTRACT`, 변경 후 활성 키 수와 한 박 경고를 함께 표시한다.
- 축소로 F 재배치가 발생하면 `RECENTER F` 피드백을 표시하되 피격·콤보 초기화처럼 보이지 않게 구분한다.
- 회복 예고의 파랑/초록은 위험의 빨강과 구분하되 색만으로 의미를 전달하지 않는다.
- COUNTDOWN은 큰 숫자 `3`, `2`, `1`과 박자 펄스를 함께 사용하며 PLAYING HUD와 구분한다.
- 예고는 충분히 밝지만 충돌보다 덜 강해야 한다.
- 충돌 순간에는 짧은 스케일/글로우 변화로 박자를 강조한다.
- 피격 플래시와 화면 흔들림은 짧게 제한한다.
- 색각과 모션 민감도를 고려해 색 이외의 단서 및 reduced-motion 대안을 제공한다.

### 사운드

- 예고 틱, 충돌 임팩트, 이동, 성공 회피, 피격, 시작/종료 큐를 구분한다.
- 카운트다운 틱은 선택한 스테이지의 오프닝 BPM 한 박과 일치하고 마지막 PLAYING 진입 큐를 구분한다.
- 플레이 중 틱과 펄스는 현재 BPM 변화를 따라가며 섹션 전환과 존 변경에는 공격음과 구분되는 짧은 큐를 사용한다.
- 배경 리듬이 없어도 핵심 박자를 알 수 있어야 한다.
- 브라우저 자동 재생 정책 때문에 첫 클릭 또는 키 입력 뒤 오디오를 활성화한다.
- 음소거 상태에서도 모든 타이밍 정보가 시각적으로 전달돼야 한다.

## 13. 프로토타입 범위

### 반드시 포함

- 별도 TITLE과 `Space`/`Enter`/클릭 시작
- 처음부터 모두 선택 가능한 STAGE 01~06 선택
- 초기 강조 없음, 물리 `Q/W/E/R/T/Y` 및 카드 클릭의 2단계 강조·확정
- 화살표 강조 이동과 `Enter` 확정
- 스테이지 오프닝 BPM 한 박 간격의 3→2→1 COUNTDOWN
- 12/14/16/18/21/24웨이브의 여섯 고정 시퀀스와 스테이지별 오프닝·최저·최고 BPM 설정
- 모든 스테이지의 `OPENING → EXPANSION/SPEED-UP → CONTRACTION/SLOWDOWN → LARGER/FASTER FINALE` 템포·존 전개
- 패턴별 5–26키 활성 존 확장·축소, 모든 존의 F 포함과 축소 시 안전 재집결/한 박 예고
- QWERTY 26셀 기반 렌더링과 비활성 셀 구분
- `KeyboardEvent.code` 기반 즉시 이동
- F 시작 위치
- 박자 기반 공격 예고와 셀 충돌
- HP 5, 500ms 무적
- STAGE 03~06의 하나만 안전한 공격
- STAGE 03~06의 희귀 회복 웨이브 각 1회
- 콤보와 최고 콤보
- TITLE/STAGE_SELECT/COUNTDOWN/PLAYING/PAUSED/CLEARED/GAME_OVER 흐름
- HUD의 현재 BPM·템포 섹션·활성 키 수·존 변경 예고
- 결과 화면의 RETRY/STAGE SELECT/조건부 NEXT STAGE와 재진입 카운트다운
- 기본 효과음과 음소거
- 재시작

### 이번 프로토타입에서 제외

- 회원가입, 클라우드 저장, 온라인 순위표
- 스토리, 캐릭터 선택, 성장 및 상점
- 곡 선택과 외부 음원 동기화
- 모바일 터치용 별도 조작 체계
- 픽셀 단위 이동과 물리 충돌
- 숫자키·특수키·방향키를 이용한 플레이 중 능력
- 맵 편집기와 사용자 제작 패턴

## 14. 플레이테스트 성공 기준

첫 내부 플레이테스트에서 다음을 관찰한다.

- 10초 안에 “누른 키로 이동한다”는 규칙을 이해하는가.
- TITLE을 게임 화면과 혼동하지 않고 `Space`/`Enter`/클릭으로 STAGE_SELECT에 진입하는가.
- STAGE_SELECT의 초기 무선택 상태와 카드 강조를 구분하는가.
- 물리 Q/W/E/R/T/Y와 카드 클릭이 첫 입력에는 강조만, 같은 대상의 두 번째 입력에는 확정한다는 점을 이해하는가.
- 화살표로 강조를 옮기고 Enter로 확정하는 보조 동선을 발견하는가.
- 스테이지 선택에서 오프닝/범위 BPM, 활성 키 범위와 웨이브 수 차이를 이해하는가.
- 3→2→1이 선택한 스테이지의 오프닝 BPM으로 느껴지고 첫 공격 준비에 도움이 되는가.
- HUD의 현재 BPM과 섹션명을 보고 감속·가속·러시를 예측할 수 있는가.
- 활성 키 수와 `ZONE EXPAND`/`ZONE CONTRACT` 예고를 보고 이동 가능한 영역 변화를 이해하는가.
- 축소 전에 존 밖에 있던 플레이어가 F로 재배치되고 한 박의 준비 시간 뒤 공격받는 과정을 공정하다고 느끼는가.
- 공격 예고만 보고 충돌 시점을 예측할 수 있는가.
- `TARGET / MOVE!`, `ONLY SAFE`, `HEAL`을 색 없이도 구분하는가.
- 실패 원인을 자신의 위치 선택으로 납득하는가.
- 여섯 스테이지의 오프닝 92→132 BPM, 82–156 전체 변화 범위와 5–26키 존 변주가 자연스럽고 각 스테이지 길이가 지루하거나 불공정하지 않은가.
- 하나만 안전한 공격이 반응 가능하고, 회복의 입력+위치 조건이 이해되는가.
- 회복 출현 빈도가 특별하게 느껴지면서도 플레이 중 한 번은 알아볼 수 있는가.
- 다시 시도할 때 더 나아질 수 있다는 느낌이 드는가.
- 결과 화면에서 재도전, 스테이지 선택, 조건부 다음 스테이지의 차이를 바로 이해하고 재도전/NEXT 뒤 카운트다운을 예상하는가.
- 카운트다운 중 Escape와 포커스 상실 뒤 공격이나 자동 시작 없이 선택 화면으로 돌아가는가.
- 한글 입력 상태, 낮은 프레임률, 탭 전환에서도 판정이 일관적인가.

핵심 루프가 재미없다면 콘텐츠를 늘리기 전에 이동 방식, 예고 길이, 패턴 가독성, 한 판 길이를 먼저 조정한다.
