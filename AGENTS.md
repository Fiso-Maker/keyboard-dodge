# Keyboard Dodge 개발 지침

## 프로젝트 목표

Keyboard Dodge는 실제 QWERTY 키보드의 공간 기억을 이용하는 스테이지형 리듬 회피 웹 게임이다. 프로토타입의 최우선 질문은 하나다.

> 알파벳 키를 눌러 키보드 위 위치를 즉시 바꾸고, 박자에 맞춰 예고되는 공격을 피하는 행동이 재미있는가?

기능 수보다 이 핵심 루프의 입력 반응성, 공격 가독성, 공정한 판정을 우선한다.

## 문서 우선순위

- 사용자의 최신 요청이 항상 최우선이다.
- 게임 규칙과 경험의 기준은 `GAME_DESIGN.md`다.
- 확정된 선택과 변경 이유는 `DECISIONS.md`에 기록한다.
- 작업 순서와 완료 조건은 `ROADMAP.md`에서 관리한다.
- 구현이 기획과 달라져야 한다면 조용히 어긋나게 두지 말고 관련 문서를 같은 변경에서 갱신한다.

## 기술 기준

- 브라우저 기반 React + TypeScript 게임으로 구현한다.
- 키 입력은 문자 값인 `KeyboardEvent.key`가 아니라 물리 위치인 `KeyboardEvent.code`를 사용한다.
- 플레이 중 유효한 이동 입력은 `KeyA`부터 `KeyZ`까지이며 `event.repeat === true`는 무시한다.
- 메뉴 단축키도 문자 값이 아닌 `KeyboardEvent.code`를 사용한다. 타이틀은 `Space`/`Enter`, 스테이지 강조는 물리 `KeyQ`/`KeyW`/`KeyE`/`KeyR`/`KeyT`/`KeyY`, 보조 탐색은 화살표, 확정은 같은 Q/W/E/R/T/Y의 두 번째 입력 또는 `Enter`다.
- 게임 시간과 공격 판정은 단조 시계(`performance.now()` 계열)를 기준으로 계산한다. 프레임 수나 누적 `setInterval` 호출 횟수에 게임 결과를 의존시키지 않는다.
- 키 셀 식별자는 문자열 하나로 통일하고, 플레이어 위치와 공격 대상 모두 같은 타입을 사용한다.
- 화면에 보이는 키 배열과 입력 판정에 쓰는 키 배열은 하나의 공통 데이터에서 파생한다.
- 핵심 규칙과 패턴 생성은 가능한 한 순수 함수로 분리해 테스트할 수 있게 한다.
- 프로토타입에는 서버, 데이터베이스, 로그인, 순위표를 추가하지 않는다.
- 새로운 런타임 의존성은 핵심 루프에 명확한 이득이 있을 때만 추가한다.

## 게임 불변 조건

- 시작 위치는 `KeyF`다.
- 마지막으로 누른 유효 알파벳 키가 현재 위치이며 이동은 즉시 완료된다.
- 인접 키 이동 제한은 없다.
- 공격은 픽셀 충돌이 아니라 충돌 순간의 Key Cell 포함 여부로 판정한다.
- 최대 HP는 5이며 피격 뒤 500ms 동안 추가 피해를 받지 않는다.
- 생존 판정은 충돌 순간 안전한 셀에 있었는지만 본다. 입력의 빠름/늦음 같은 리듬 평가는 생존 결과를 바꾸지 않는다.
- 화면 흐름은 `TITLE → STAGE_SELECT → COUNTDOWN → PLAYING ⇄ PAUSED → CLEARED/GAME_OVER` 순서를 따른다.
- STAGE_SELECT의 초기 강조 카드는 없다. Q/W/E/R/T/Y나 카드 클릭의 첫 입력은 강조만 하고, 같은 대상의 두 번째 입력이 확정한다. 화살표는 강조를 이동하고 Enter는 강조된 카드만 확정한다.
- 확정 후 선택한 스테이지의 **오프닝 BPM** 한 박마다 `3 → 2 → 1`을 표시한 다음 PLAYING에 진입한다. RETRY와 NEXT도 대상 스테이지의 오프닝 BPM 카운트다운을 거치며, 카운트다운 중 Escape 또는 포커스 상실은 STAGE_SELECT로 안전하게 복귀한다.
- STAGE 01~06은 처음부터 모두 선택 가능하며 물리 Q/W/E/R/T/Y에 순서대로 대응한다. 각 스테이지는 하나의 고정 BPM이나 키 수가 아니라 오프닝·감속·가속·러시 템포 구간과 패턴별 활성 키 존 변화를 가진다.
- STAGE 01~06의 `웨이브 / 오프닝 BPM / BPM 범위 / 활성 키 범위`는 각각 `12/92/82–110/5–11`, `14/100/88–122/8–15`, `16/108/94–132/10–19`, `18/116/102–142/12–22`, `21/124/108–150/15–25`, `24/132/116–156/18–26`이다.
- 각 스테이지는 `OPENING → EXPANSION/SPEED-UP → CONTRACTION/SLOWDOWN → LARGER/FASTER FINALE`의 네 구간을 순서대로 가진다.
- 모든 활성 키 존은 `KeyF`를 포함한다. 존이 축소될 때 플레이어가 새 존 밖에 있으면 먼저 `KeyF`로 재배치하고, 그 뒤 최소 한 박의 존 변경 예고를 완료하기 전에는 다음 공격을 판정하지 않는다.
- 공격 대상과 안전 셀 계산은 해당 웨이브의 현재 활성 키 존만 사용하며, 모든 공격은 그 존 안에 최소 한 개 이상의 안전 셀을 남긴다.
- STAGE 03~06에는 현재 존에서 키 하나만 안전한 공격과, 예고 중 목표 키를 직접 입력한 뒤 판정 순간 그 셀에 있으면 HP를 1 회복하는 희귀 패턴을 포함한다. 회복은 해당 스테이지마다 1회다.
- 결과 화면은 같은 스테이지 재도전과 스테이지 선택 복귀를 항상 제공한다. 다음 스테이지는 STAGE 01~05 클리어 때만 제공하며 재도전과 다음 스테이지도 반드시 카운트다운을 거친다.
- 일시정지 중에는 타이머, 공격, 콤보가 진행되지 않는다.
- 템포 변화, 존 변경, 공격 판정은 같은 단조 타임라인에서 순서가 결정돼야 한다. 일시정지 후 재개해도 현재 BPM, 섹션, 존과 다음 이벤트의 상대 간격이 유지돼야 한다.

## UX 기준

- 공격 상태는 최소 `예고 → 충돌 → 해제`의 시각 단계가 구분돼야 한다.
- 타이틀은 스테이지 선택이나 게임 HUD가 비치지 않는 별도 시작 화면이어야 하며 `Space`, `Enter`, 클릭으로 진입할 수 있어야 한다.
- 스테이지 선택은 초기 강조 없이 시작한다. 물리 `Q/W/E/R/T/Y`와 카드 클릭은 첫 입력으로 강조, 같은 대상의 두 번째 입력으로 확정하고, 화살표+`Enter`는 강조 이동과 확정을 분리한다.
- 카운트다운은 선택한 스테이지의 오프닝 BPM을 시각·음향으로 예고하되 플레이 타이머, 공격, 이동, 콤보를 시작하지 않는다.
- HUD에는 현재 BPM, 현재 활성 키 수, 템포 섹션과 존 변경 예고를 실시간으로 표시한다. 감속·가속·러시 및 확장·축소는 색뿐 아니라 텍스트와 형태로도 구분한다.
- 색만으로 위험, 피격, 현재 위치를 전달하지 않는다. 형태, 테두리, 라벨 또는 움직임을 함께 사용한다.
- 화면 흔들림과 강한 플래시는 짧고 절제하며 `prefers-reduced-motion`을 존중한다.
- 사운드는 첫 사용자 상호작용 뒤에 시작하고, 항상 음소거 토글을 제공한다.
- 브라우저 포커스를 잃으면 입력이 꼬이지 않도록 자동 일시정지한다.
- 모바일 화면에는 데스크톱 키보드 플레이가 필요하다는 안내를 제공할 수 있지만, 1차 목표 플랫폼은 물리 키보드가 있는 데스크톱 브라우저다.

## 변경 방식

- 관련 없는 스타터 코드, 사용자 변경, 설정을 함께 정리하지 않는다.
- 작은 단위로 구현하고 각 단계에서 핵심 루프를 직접 플레이해 확인한다.
- UI 텍스트와 접근성 이름은 실제 게임 상태와 일치시킨다.
- 스테이지별 오프닝 BPM, 템포 구간, 활성 키 존 순서, 웨이브 수와 난이도 수치는 하드코딩해 흩뿌리지 말고 한 곳의 설정으로 모은다.
- 새 패턴에는 적용 템포, 활성 존, 공격 대상, 예고 시점, 충돌 시점, 안전 셀 존재 여부를 검증하는 테스트를 우선한다.
- 존 정의와 패턴 데이터는 별도 진실 공급원을 만들지 않는다. 렌더링, 이동 필터, 공격 생성과 HUD는 현재 웨이브에서 계산된 같은 존을 사용한다.

## 검증

변경 범위에 맞춰 아래 명령을 실행한다.

```bash
pnpm run lint
pnpm test
pnpm run build
```

게임 로직 변경 시 자동 검사만으로 끝내지 말고 다음을 수동 확인한다.

1. 한글 IME 상태에서도 물리 알파벳 위치로 이동하는가.
2. 길게 누른 키가 반복 이동이나 콤보 증가를 만들지 않는가.
3. 예고가 끝나는 정확한 충돌 순간의 플레이어 셀로 피격을 판정하는가.
4. 피격 후 500ms 동안 중첩 피해가 차단되는가.
5. 일시정지와 탭 전환 뒤 타이머 및 다음 공격 시점이 튀지 않는가.
6. TITLE에서 `Space`/`Enter`/클릭이 STAGE_SELECT로만 이동하고 즉시 게임을 시작하지 않는가.
7. STAGE_SELECT 진입 시 어떤 카드도 강조되지 않는가.
8. 물리 Q/W/E/R/T/Y와 카드 클릭은 첫 입력에 강조만 하고 같은 대상의 두 번째 입력에 확정하는가.
9. 화살표는 강조만 이동하고 Enter는 강조된 카드가 있을 때만 확정하는가.
10. 확정 뒤 STAGE 01~06 각각의 오프닝 BPM 한 박 간격으로 3→2→1을 모두 표시한 후에만 PLAYING이 시작되는가.
11. 카운트다운 중 Escape 또는 포커스 상실 시 공격·타이머 없이 STAGE_SELECT로 복귀하는가.
12. STAGE 01~06이 물리 Q/W/E/R/T/Y에 정확히 대응하고 각각 12/14/16/18/21/24웨이브, 오프닝 92/100/108/116/124/132 BPM과 설정된 BPM·키 수 범위를 사용하는가.
13. 마지막 웨이브 돌파와 HP 0 종료가 각각 올바른 결과 화면으로 이어지는가.
14. 결과의 재도전과 조건부 다음 스테이지가 카운트다운을 거치고, 스테이지 선택은 안전하게 STAGE_SELECT로 복귀하는가.
15. 감속·가속·러시 구간에서 HUD의 현재 BPM과 실제 예고·판정 간격이 일치하는가.
16. 패턴에 따라 활성 키 존이 확장·축소되며 이동 필터, 화면 표시, 공격 대상과 안전 셀 계산이 같은 존을 사용하는가.
17. 모든 존에 `KeyF`가 있고, 축소 시 플레이어가 새 존 밖이면 F로 재배치된 뒤 한 박의 변경 예고가 끝나기 전에는 공격이 판정되지 않는가.
18. 일시정지와 탭 전환 뒤 현재 템포 구간, BPM, 활성 존과 다음 이벤트 간격이 튀지 않는가.
