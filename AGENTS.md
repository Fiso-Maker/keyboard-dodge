# Keyboard Dodge 개발 지침

## 프로젝트 목표

Keyboard Dodge는 실제 QWERTY 키보드의 공간 기억을 이용하는 스테이지형 리듬 회피 웹 게임이다. 프로토타입의 최우선 질문은 하나다.

> 알파벳 키를 눌러 키보드 위 위치를 즉시 바꾸고, 박자에 맞춰 예고되는 공격을 피하는 행동이 재미있는가?

기능 수보다 이 핵심 루프의 입력 반응성, 공격 가독성, 공정한 판정을 우선한다.

## 문서 우선순위

- 사용자의 최신 요청이 항상 최우선이다.
- 게임 규칙과 경험의 기준은 `GAME_DESIGN.md`다.
- 확정된 선택과 변경 이유는 `DECISIONS.md`에 기록한다.
- 작업 순서와 완료 조건은 `ROADMAP.md`에서 관리한다.
- 구현이 기획과 달라져야 한다면 조용히 어긋나게 두지 말고 관련 문서를 같은 변경에서 갱신한다.

## 기술 기준

- 브라우저 기반 React + TypeScript 게임으로 구현한다.
- 키 입력은 문자 값인 `KeyboardEvent.key`가 아니라 물리 위치인 `KeyboardEvent.code`를 사용한다.
- 플레이 중 유효한 이동 입력은 `KeyA`부터 `KeyZ`까지이며 `event.repeat === true`는 무시한다.
- 메뉴 단축키도 문자 값이 아닌 `KeyboardEvent.code`를 사용한다. 타이틀은 `Space`/`Enter`, 스테이지 강조는 물리 `KeyQ`/`KeyW`/`KeyE`/`KeyR`/`KeyT`/`KeyY`, 보조 탐색은 화살표, 확정은 같은 Q/W/E/R/T/Y의 두 번째 입력 또는 `Enter`다.
- 게임 시간과 공격 판정은 단조 시계(`performance.now()` 계열)를 기준으로 계산한다. 프레임 수나 누적 `setInterval` 호출 횟수에 게임 결과를 의존시키지 않는다.
- 절차형 음악은 Web Audio의 별도 전송 시계로 미리 예약하되 게임의 `performance.now()` 타임라인을 따라가는 출력 계층으로만 둔다. 오디오 재생 위치가 공격, 충돌, 웨이브 또는 상태 전환을 앞당기거나 결정해서는 안 된다.
- 키 셀 식별자는 문자열 하나로 통일하고, 플레이어 위치와 공격 대상 모두 같은 타입을 사용한다.
- 화면에 보이는 키 배열과 입력 판정에 쓰는 키 배열은 하나의 공통 데이터에서 파생한다.
- 핵심 규칙과 패턴 생성은 가능한 한 순수 함수로 분리해 테스트할 수 있게 한다.
- 프로토타입에는 서버, 데이터베이스, 로그인, 순위표를 추가하지 않는다.
- 런 기록은 브라우저 `localStorage`에만 저장한다. 같은 런을 두 번 집계하지 않도록 고유 런 ID와 최근 커밋 ID를 사용하며, 클리어·게임 오버의 모든 종료 경로는 하나의 중앙 완료 함수만 거친다.
- 새로운 런타임 의존성은 핵심 루프에 명확한 이득이 있을 때만 추가한다.

## 게임 불변 조건

- 시작 위치는 `KeyF`다.
- 마지막으로 누른 유효 알파벳 키가 현재 위치이며 이동은 즉시 완료된다.
- 인접 키 이동 제한은 없다.
- 공격은 픽셀 충돌이 아니라 충돌 순간의 Key Cell 포함 여부로 판정한다.
- 최대 HP는 5이며 피격 뒤 500ms 동안 추가 피해를 받지 않는다.
- 생존 판정은 충돌 순간 안전한 셀에 있었는지만 본다. 입력의 빠름/늦음 같은 리듬 평가는 생존 결과를 바꾸지 않는다.
- 화면 흐름은 `TITLE → STAGE_SELECT → COUNTDOWN → PLAYING ⇄ PAUSED → CLEARED/GAME_OVER` 순서를 따른다.
- STAGE_SELECT의 초기 강조 카드는 없다. 카드는 왼쪽에서 오른쪽으로 이어진 가로 스크롤 덱으로 배치한다. Q/W/E/R/T/Y나 카드 클릭의 첫 입력은 해당 카드를 강조하고 덱 중앙으로 스크롤하며 앞으로 튀어나오게 한다. 같은 대상의 두 번째 입력이 확정한다. 화살표는 인접 카드로 강조를 옮기고 중앙에 노출하며, Enter는 강조된 카드만 확정한다.
- 확정 후 선택한 스테이지의 **오프닝 BPM** 한 박마다 `3 → 2 → 1`을 표시한 다음 PLAYING에 진입한다. RETRY와 NEXT도 대상 스테이지의 오프닝 BPM 카운트다운을 거치며, 카운트다운 중 Escape 또는 포커스 상실은 STAGE_SELECT로 안전하게 복귀한다.
- 카운트다운은 키보드를 가리는 전면 오버레이가 아니라 키보드 위의 작은 준비 배지로 표시한다. 이 동안 오프닝 활성 존의 키로 위치를 미리 정할 수 있지만 공격, 점수, 콤보, 웨이브와 플레이 타이머는 시작하지 않는다.
- STAGE 01~06은 처음부터 모두 선택 가능하며 물리 Q/W/E/R/T/Y에 순서대로 대응한다. 각 스테이지는 하나의 고정 BPM이나 키 수가 아니라 오프닝·감속·가속·러시 템포 구간과 패턴별 활성 키 존 변화를 가진다.
- 로컬 기록은 스테이지 잠금이나 진행 게이트로 사용하지 않는다. 각 스테이지의 시도 수, 클리어 수, 최고 도달 웨이브, 최고 점수, 최고 콤보, 클리어 최고 잔여 HP와 최고 등급·레이팅만 기기 안에 보존한다.
- STAGE 01~06의 `웨이브 / 오프닝 BPM / BPM 범위 / 활성 키 범위`는 각각 `52/92/82–110/5–11`, `56/100/88–122/8–15`, `60/108/94–132/10–19`, `64/116/102–142/12–22`, `68/124/108–150/15–25`, `72/132/116–156/18–26`이다. 판정 잔상과 위험 기반 전환을 포함한 논리 시퀀스 길이는 약 113–116초로 2분에 가깝다.
- 각 스테이지는 `OPENING → EXPANSION/SPEED-UP → CONTRACTION/SLOWDOWN → LARGER/FASTER FINALE`의 네 구간을 순서대로 가진다.
- 두 번째 ACT부터 각 섹션 시작을 `ACT n / 4` 전환 큐로 알린다. 마지막 ACT는 `FINAL SEQUENCE`로 별도 강조하며, 플레이 화면의 진행선과 강조 요소는 선택한 스테이지의 고유 식별색을 사용한다.
- 모든 활성 키 존은 `KeyF`를 포함한다. 존 목표 크기는 기본적으로 5웨이브 펄스마다 변하며, 존 전환은 직전 공격이 끝난 뒤 다음 공격과 분리된 전용 예고를 거친다.
- 축소 후보는 직전 공격에서 실제 위험 셀이었던 활성 키로만 고른다(`KeyF` 제외). UI는 `COLLAPSE WARNING`으로 후보 셀을 별도 형태와 라벨로 1.5박 예고하고, 적용 뒤 `FIELD COLLAPSED`를 표시한 다음 비활성화한다. 위험 후보가 부족하면 임의의 안전 셀을 추가로 없애지 않는다.
- 확장은 `RESTORE WARNING`으로 새로 열릴 셀을 1.25박 예고하고, 적용 뒤 `ZONE RESTORED`를 표시한 다음 활성화한다. 전환 예고 중에는 다음 공격을 판정하지 않는다.
- 축소 적용 순간 플레이어가 붕괴 셀에 남아 있으면 피해 1을 시도한다. 일반 공격과 같은 500ms 무적 타이머를 공유하므로 이미 무적이면 HP를 추가로 깎지 않고 무적 종료 시각도 늘리지 않는다. 피해 적용 여부와 무관하게 해당 셀은 비활성화하고 플레이어를 남은 활성 키 중 물리적으로 가장 가까운 셀로 즉시 재배치한다.
- 가장 가까운 셀은 화면에 보이는 공통 QWERTY 키 중심 좌표의 제곱 유클리드 거리로 결정한다. 거리가 같으면 공통 `ALL_KEYS` 행 우선·왼쪽 우선 순서에서 먼저 나오는 키를 선택해 재시도와 테스트가 동일하게 재현되게 한다.
- 공격 대상과 안전 셀 계산은 해당 웨이브의 현재 활성 키 존만 사용하며, 모든 공격은 그 존 안에 최소 한 개 이상의 안전 셀을 남긴다.
- `WAVE SURGE`는 0부터 세는 `waveIndex 5`에서 처음 시작하고 이후 13웨이브마다 3연속 일반 공격으로 재생한다. 시퀀스 방향은 왼쪽→오른쪽과 오른쪽→왼쪽을 번갈아 사용하며, 각 웨이브의 파도 대상은 그 순간의 런타임 활성 존에서 다시 계산하고 최소 2개의 안전 키를 남긴다.
- STAGE 03~06에는 현재 존에서 키 하나만 안전한 공격과, 예고 중 목표 키를 직접 입력한 뒤 판정 순간 그 셀에 있으면 HP를 1 회복하는 희귀 패턴을 포함한다. 회복은 해당 스테이지마다 1회다.
- 결과 화면은 같은 스테이지 재도전과 스테이지 선택 복귀를 항상 제공한다. 다음 스테이지는 STAGE 01~05 클리어 때만 제공하며 재도전과 다음 스테이지도 반드시 카운트다운을 거친다.
- 클리어와 게임 오버 결과는 등급, 0–100 레이팅, 점수, 최고 콤보, 잔여 HP, 도달 웨이브와 첫 클리어·신기록 배지를 한 화면에 보여준다. 해당 런은 결과 화면에 진입하기 전에 정확히 한 번 로컬 기록에 반영한다.
- 인터페이스 테마는 **POLAR WHITE**(기본), **WARM WHITE**, **MONO SIGNAL**, **SIGNAL BLUE**, **AMBER OPS**, **IVORY LAB**의 정확히 여섯 개다. 테마 변경은 시각 토큰만 바꾸며 타이머, 입력, 공격, BPM, 활성 존, HP, 점수, 콤보와 게임 상태를 변경하거나 초기화하지 않는다.
- 테마 설정은 TITLE, STAGE_SELECT와 플레이 화면에서 열 수 있다. PLAYING 중 열면 먼저 기존 일시정지 경로로 안전하게 PAUSED가 되고, 닫은 뒤에는 사용자가 명시적으로 재개한다. COUNTDOWN 중에는 설정 진입을 비활성화한다.
- 테마 선택 버튼은 적용 전에도 각 테마의 대표 배경, 패널 형태, 타이포그래피, QWERT 키캡과 팔레트를 미니 프리뷰로 보여줘야 한다.
- 일시정지 중에는 타이머, 공격, 콤보가 진행되지 않는다.
- 템포 변화, 존 변경, 공격 판정은 같은 단조 타임라인에서 순서가 결정돼야 한다. 일시정지 후 재개해도 현재 BPM, 섹션, 존과 다음 이벤트의 상대 간격이 유지돼야 한다.
- 배경 스코어는 외부 녹음·샘플·기성 작곡을 사용하지 않는 런타임 Web Audio 합성물이다. STAGE 01~06은 서로 다른 모티프를 가지며, 4/4의 킥·펄스·하이햇·베이스·모티프 레이어가 현재 웨이브의 ACT, BPM과 강도를 따른다.
- 스코어는 카운트다운, 플레이 시작, 일시정지, 재개, 음소거, 결과·스테이지 이탈과 컴포넌트 해제 수명주기에 맞춰 시작·정지한다. 결과 화면이나 취소된 카운트다운 뒤에 예약 음이 남아서는 안 된다.

## UX 기준

- 공격 상태는 최소 `예고 → 충돌 → 해제`의 시각 단계가 구분돼야 한다.
- 타이틀은 스테이지 선택이나 게임 HUD가 비치지 않는 별도 시작 화면이어야 하며 `Space`, `Enter`, 클릭으로 진입할 수 있어야 한다.
- 타이틀은 로고, 한 줄 경험 설명과 시작 동작에 집중한다. 스테이지 수, 키 수, BPM 같은 기술 수치는 타이틀에 표시하지 않고 선택 카드와 플레이 HUD에서만 제공한다.
- 스테이지 선택은 초기 강조 없는 왼쪽→오른쪽 가로 스크롤 카드 덱으로 시작한다. 물리 `Q/W/E/R/T/Y`와 카드 클릭은 첫 입력으로 해당 카드를 중앙에 스크롤하고 앞으로 튀어나오게 강조한다. 같은 대상의 두 번째 입력으로 확정하고, 화살표+`Enter`는 인접 카드 이동과 확정을 분리한다.
- 카운트다운은 선택한 스테이지의 오프닝 BPM을 시각·음향으로 예고한다. 키보드 위의 작은 배지로 3→2→1을 표시해 보드를 계속 볼 수 있게 하고, 오프닝 활성 키 이동만 허용하되 플레이 타이머, 공격, 점수, 웨이브와 콤보는 시작하지 않는다.
- 상단 가로 계기판형 HUD에는 스테이지, 웨이브, 점수, HP, 현재 BPM, 현재 활성 키 수, 템포 섹션과 진행도를 실시간으로 표시한다. 감속·가속·러시 및 확장·축소는 색뿐 아니라 텍스트와 형태로도 구분한다.
- 섹션 경계의 ACT 큐와 마지막 `FINAL SEQUENCE`는 키보드와 공격 라벨을 가리지 않는 진행 영역에 표시하고, 색 외에 ACT 번호·섹션명·상태 문구로도 구분한다.
- `WAVE SURGE` 중에는 진행 방향과 3연속 중 현재 순서를 텍스트·화살표 readout과 키보드의 방향성 신호로 함께 표시한다. 이 단서는 현재 위치와 이동 필요 여부를 읽을 수 있어야 하며 테마 색 하나에 의존하지 않는다.
- 영구 측면 콤보 통계는 두지 않는다. 성공적인 연속 회피는 키보드 바깥 상단 HUD의 우측 진행도 칸에 일시적으로 표시하고 애니메이션 뒤 실제로 제거하며, 키 캡과 공격 정보를 가리지 않는다. 최고 콤보는 결과 화면에 남긴다.
- 여섯 테마 모두 같은 정보 구조, 라벨과 색 이외의 상태 단서를 유지한다. STAGE_SELECT의 왼쪽→오른쪽 가로 스크롤·중앙 팝업 카드 덱은 테마와 무관한 불변 UX다.
- 색만으로 위험, 피격, 현재 위치를 전달하지 않는다. 형태, 테두리, 라벨 또는 움직임을 함께 사용한다.
- 화면 흔들림과 강한 플래시는 짧고 절제하며 `prefers-reduced-motion`을 존중한다.
- 효과음과 절차형 스코어는 첫 사용자 상호작용 뒤에 시작하고, 항상 음소거 토글을 제공한다. 음소거 상태에서도 모든 박자·방향·판정 정보가 화면에 남아야 한다.
- 브라우저 포커스를 잃으면 입력이 꼬이지 않도록 자동 일시정지한다.
- 모바일 화면에는 데스크톱 키보드 플레이가 필요하다는 안내를 제공할 수 있지만, 1차 목표 플랫폼은 물리 키보드가 있는 데스크톱 브라우저다.

## 변경 방식

- 관련 없는 스타터 코드, 사용자 변경, 설정을 함께 정리하지 않는다.
- 작은 단위로 구현하고 각 단계에서 핵심 루프를 직접 플레이해 확인한다.
- UI 텍스트와 접근성 이름은 실제 게임 상태와 일치시킨다.
- 스테이지별 오프닝 BPM, 템포 구간, 활성 키 존 순서, 5웨이브 존 펄스, 전환 예고 박 수, 웨이브 수와 난이도 수치는 하드코딩해 흩뿌리지 말고 한 곳의 설정으로 모은다.
- `WAVE SURGE`의 시작·주기·3웨이브 길이·방향과 스테이지별 음악 모티프·레이어 편성은 타입이 지정된 설정과 순수 계획 함수에서 관리한다.
- 공격 간격은 16분음표 그리드에 해당하는 0.25박 단위로 저작한다. 존 전환처럼 임의 길이의 판정 잔상이 끼는 경계에서는 음악을 게임 전환 시점에 단방향 재동기화하되, 오디오는 공격이나 충돌을 진행시키지 않는다.
- 테마 ID, 표시명, 밝기 모드와 선택 화면용 팔레트는 하나의 타입이 지정된 설정에서 관리하고, 화면별 색상은 공통 테마 토큰에서 파생한다.
- 새 패턴에는 적용 템포, 활성 존, 공격 대상, 예고 시점, 충돌 시점, 안전 셀 존재 여부를 검증하는 테스트를 우선한다.
- 존 정의와 패턴 데이터는 별도 진실 공급원을 만들지 않는다. 렌더링, 이동 필터, 공격 생성과 HUD는 현재 웨이브에서 계산된 같은 존을 사용한다.

## 검증

변경 범위에 맞춰 아래 명령을 실행한다.

```bash
pnpm run typecheck
pnpm run lint
pnpm test
pnpm run build
```

게임 로직 변경 시 자동 검사만으로 끝내지 말고 다음을 수동 확인한다.

1. 한글 IME 상태에서도 물리 알파벳 위치로 이동하는가.
2. 길게 누른 키가 반복 이동이나 콤보 증가를 만들지 않는가.
3. 예고가 끝나는 정확한 충돌 순간의 플레이어 셀로 피격을 판정하는가.
4. 피격 후 500ms 동안 중첩 피해가 차단되는가.
5. 일시정지와 탭 전환 뒤 타이머 및 다음 공격 시점이 튀지 않는가.
6. TITLE에서 `Space`/`Enter`/클릭이 STAGE_SELECT로만 이동하고 즉시 게임을 시작하지 않는가.
7. STAGE_SELECT 진입 시 어떤 카드도 강조되지 않는가.
8. 물리 Q/W/E/R/T/Y와 카드 클릭은 첫 입력에 대상 카드를 강조·중앙 스크롤·팝업하고 같은 대상의 두 번째 입력에 확정하는가.
9. 화살표는 가로 덱의 인접 카드로 강조를 옮기고 중앙에 노출하며, Enter는 강조된 카드가 있을 때만 확정하는가.
10. 확정 뒤 STAGE 01~06 각각의 오프닝 BPM 한 박 간격으로 3→2→1을 모두 표시한 후에만 PLAYING이 시작되며, 작은 배지가 키보드를 가리지 않는가.
11. 카운트다운 중 Escape 또는 포커스 상실 시 공격·타이머 없이 STAGE_SELECT로 복귀하는가.
12. 카운트다운 중 오프닝 활성 키 이동은 가능하지만 공격·점수·콤보·웨이브·플레이 타이머는 진행되지 않는가.
13. STAGE 01~06이 물리 Q/W/E/R/T/Y에 정확히 대응하고 각각 52/56/60/64/68/72웨이브, 약 113–116초 길이, 오프닝 92/100/108/116/124/132 BPM과 설정된 BPM·키 수 범위를 사용하는가.
14. 마지막 웨이브 돌파와 HP 0 종료가 각각 올바른 결과 화면으로 이어지는가.
15. 결과의 재도전과 조건부 다음 스테이지가 카운트다운을 거치고, 스테이지 선택은 안전하게 STAGE_SELECT로 복귀하는가.
16. 감속·가속·러시 구간에서 HUD의 현재 BPM과 실제 예고·판정 간격이 일치하는가.
17. 5웨이브 펄스와 섹션 경계에서 활성 키 존이 확장·축소되며 이동 필터, 화면 표시, 공격 대상과 안전 셀 계산이 같은 런타임 존을 사용하는가.
18. 축소는 직전 공격 위험 키만 후보로 삼아 `COLLAPSE WARNING` 1.5박 뒤 적용하고, 확장은 `RESTORE WARNING` 1.25박 뒤 적용하며, 전환 예고가 끝나기 전 다음 공격이 판정되지 않는가.
19. 붕괴 후보 셀에 있던 플레이어가 경고 중 이동할 수 있고, 적용 순간에도 남아 있으면 공격과 공유하는 500ms 무적 규칙으로 피해 1을 판정한 뒤 가장 가까운 남은 활성 키로 재배치하는가.
20. 가장 가까운 키가 둘 이상이면 공통 QWERTY 좌표와 `ALL_KEYS` 순서에 따라 같은 결과를 선택하며, 무적으로 피해가 차단돼도 재배치는 발생하는가.
21. 일시정지와 탭 전환 뒤 현재 템포 구간, BPM, 활성 존과 다음 이벤트 간격이 튀지 않는가.
22. POLAR WHITE, WARM WHITE, MONO SIGNAL, SIGNAL BLUE, AMBER OPS, IVORY LAB이 TITLE, STAGE_SELECT와 플레이 화면에서 선택 가능하고 기본값이 POLAR WHITE인가.
23. PLAYING 중 테마 설정을 열면 먼저 일시정지되고, 설정을 닫아도 명시적 재개 전에는 타이머·입력·공격과 다음 판정이 진행되지 않는가.
24. COUNTDOWN 중 테마 설정이 비활성이고 3→2→1, 준비 이동과 예약된 PLAYING 진입에 영향을 주지 않는가.
25. 테마 전환 전후 타이머, 입력, 공격, BPM, 활성 존, HP, 점수, 콤보, 가로 스테이지 덱의 스크롤·팝업 상태가 유지되는가.
26. 상단 계기판 HUD의 수치가 실제 게임 상태와 일치하고 성공적인 연속 회피 피드백은 일시적으로만 나타나며 결과 화면의 최고 콤보는 유지되는가.
27. TITLE에 스테이지 수·키 수·BPM 기술 수치가 노출되지 않고 시작 동작과 핵심 경험만 보이는가.
28. 스테이지 선택 카드에 로컬 시도·클리어·최고 기록이 올바르게 반영되며 기록과 무관하게 STAGE 01~06이 모두 선택 가능한가.
29. 클리어와 HP 0의 모든 종료 경로가 한 런을 정확히 한 번만 커밋하고, 새로고침 뒤에도 스테이지별 기록과 등급이 복원되는가.
30. 결과 화면에 등급·레이팅·점수·최고 콤보·HP·웨이브와 해당되는 신기록 배지가 표시되며, 두 번째 ACT 이후 섹션 전환과 마지막 `FINAL SEQUENCE`가 키보드를 가리지 않고 읽히는가.
31. `WAVE SURGE`가 0부터 세는 `waveIndex 5`부터 13웨이브 주기로 정확히 3번 이어지고 방향을 번갈아 표시하며, 존이 변해도 현재 활성 키만 공격하고 최소 2개 안전 키를 남기는가.
32. 여섯 스테이지의 서로 다른 절차형 모티프와 4/4 킥·펄스·하이햇·베이스·모티프가 ACT·BPM·강도를 따라 들리고, 카운트다운·시작·일시정지·재개·음소거·결과 전환에서 중복·잔향·타임라인 이탈 없이 동작하는가.
