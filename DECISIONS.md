# DECISIONS.md — Keyboard Dodge

이 문서는 이미 선택한 설계의 이유와 향후 재검토 조건을 남긴다. 상태는 `확정`, `가설`, `보류`로 구분한다.

## D-001 — 플레이 공간은 알파벳 26키로 제한한다

- 상태: **확정**
- 결정: 표준 영문 QWERTY의 A~Z만 플레이 셀로 사용한다.
- 이유: 실제 키보드 공간 기억이라는 핵심 아이디어를 가장 익숙하고 단순한 범위에서 검증할 수 있다.
- 결과: 숫자, 기능키, 방향키, Space 등은 프로토타입 범위에서 제외한다.

## D-002 — 물리 키 위치는 `KeyboardEvent.code`로 읽는다

- 상태: **확정**
- 결정: 입력 판정은 `KeyboardEvent.key`가 아니라 `KeyboardEvent.code`를 사용한다.
- 이유: 한글 IME와 입력 언어가 바뀌어도 동일한 물리 QWERTY 위치를 유지해야 한다.
- 결과: 플레이 중 이동 코드는 `KeyA`~`KeyZ`다. 메뉴에서는 `Space`, `Enter`, `KeyQ`/`KeyW`/`KeyE`, 화살표 코드를 별도로 처리하며 반복 `keydown`은 무시한다.

## D-003 — 마지막 유효 입력이 현재 위치다

- 상태: **확정**
- 결정: 플레이어는 `KeyF`에서 시작하고, 마지막으로 누른 유효 알파벳 키로 즉시 이동한다.
- 이유: 이동 거리나 물리 시뮬레이션이 아니라 키보드 전체를 순간 탐색하는 감각을 직접 검증하기 위함이다.
- 결과: 키 홀드와 인접 이동 제한, 이동 시간, 경로 충돌은 없다.

## D-004 — 충돌은 Key Cell 포함 여부로 판정한다

- 상태: **확정**
- 결정: 충돌 순간 공격 대상 키 집합에 현재 키가 포함되는지만 검사한다.
- 이유: 화면 애니메이션과 판정을 분리해 결과를 명확하고 테스트 가능하게 유지한다.
- 결과: 픽셀 Hitbox, 캐릭터 크기, 이동 중간 경로는 프로토타입에서 고려하지 않는다.

## D-005 — HP는 5, 피격 무적은 500ms다

- 상태: **확정**
- 결정: 최대 HP 5, 피격당 피해 1, 피해 적용 뒤 500ms 무적을 사용한다.
- 이유: 초보자에게 여러 번의 학습 기회를 주면서 겹친 패턴이 한 번에 HP를 소진시키는 것을 막는다.
- 재검토 조건: 플레이테스트에서 한 판이 지나치게 관대하거나 중첩 공격의 결과가 이해되지 않을 때 수치를 조정한다.

## D-006 — 한 판의 목표는 선택한 스테이지의 고정 시퀀스 완주다

- 상태: **확정**
- 결정: 단순 45초 생존을 폐기한다. 선택한 스테이지의 마지막 고정 웨이브까지 HP가 남아 있으면 클리어하고, HP가 0이면 즉시 게임 오버다.
- 이유: 시간만 버티는 구조보다 미리 설계된 패턴을 학습하고 단계별 시퀀스를 돌파하는 목표가 진행감과 재도전 동기를 더 분명하게 준다.
- 결과: 경과 시간, 점수, 콤보는 승리 조건이 아니다. 마지막 웨이브와 HP 0이 겹치면 해당 웨이브 판정을 먼저 적용한다.

## D-007 — 리듬 구조와 생존 판정을 분리한다

- 상태: **확정**
- 결정: 공격 예고와 충돌은 박자에 놓되, 생존은 충돌 순간 안전 셀에 있는지만 판정한다.
- 이유: 리듬 입력 정확도와 공간 회피를 동시에 강제하면 실패 원인이 모호해진다. 박자는 공격 예측과 손맛을 돕는 역할에 집중한다.
- 결과: 이른 이동이나 늦은 이동에 별도 HP 페널티를 주지 않는다. 타이밍 등급을 추가하더라도 생존 결과와 분리한다.

## D-008 — 스테이지별 템포는 96/116/138 BPM이다

- 상태: **확정**
- 현재 적용: **D-023이 이 결정을 대체한다.** 아래 내용은 변경 전 이력으로 보존한다.
- 결정: STAGE 01은 96 BPM, STAGE 02는 116 BPM, STAGE 03은 138 BPM을 사용하고 각 값을 공통 스테이지 설정에서 관리한다.
- 이유: 입문 스테이지에는 읽을 여유를 주고, 활성 키와 패턴 복잡도가 늘어나는 상위 스테이지에는 체감 가능한 속도 상승을 함께 제공하기 위함이다.
- 재검토 조건: 플레이테스트에서 96 BPM이 지나치게 느리거나 138 BPM이 반응 불가능하게 느껴질 때 예고 박 수와 함께 조정한다.

## D-009 — 난이도는 선택 스테이지와 고정 웨이브 순서로 결정한다

- 상태: **확정**
- 현재 적용: 고정된 재시도 시퀀스 원칙은 유지하되, 고정 BPM·고정 키 수 부분은 **D-023과 D-024가 대체한다.**
- 결정: 스테이지가 높을수록 활성 키 수, BPM과 패턴 복잡도를 높이고, 각 스테이지 안에서는 고정된 웨이브 순서로 난이도를 전개한다.
- 이유: 같은 스테이지를 재시도할 때 실패를 학습할 수 있으면서도 상위 스테이지에서 분명한 공간·속도 확장을 제공하기 위함이다.
- 결과: 경과 시간, 콤보, 현재 HP로 공격을 동적으로 바꾸지 않는다. 같은 스테이지의 재시도는 같은 시퀀스를 재생한다.

## D-010 — 콤보는 연속 회피 피드백이다

- 상태: **확정**
- 결정: 피해 없이 공격을 통과하면 콤보 +1, 실제 피격 시 0으로 초기화한다.
- 이유: 생존이라는 단순한 목표 안에서도 숙련 향상과 재도전 성과를 보이기 위함이다.
- 결과: 무적 중 위험 셀에 남은 것은 회피 성공으로 세지 않는다. 최고 콤보는 결과 화면에 표시한다.

## D-011 — 게임 상태와 일시정지를 명시적으로 관리한다

- 상태: **확정**
- 결정: `TITLE`, `STAGE_SELECT`, `COUNTDOWN`, `PLAYING`, `PAUSED`, `CLEARED`, `GAME_OVER` 상태를 분리한다. TITLE은 게임 HUD나 스테이지 카드가 없는 완전한 별도 시작 화면이다.
- 이유: 입력, 타이머, 공격, 결과 오버레이가 동시에 진행되는 버그를 막는다.
- 결과: TITLE은 `Space`, `Enter`, 클릭으로 STAGE_SELECT에만 진입한다. 스테이지 확정, 결과의 재도전과 NEXT STAGE는 COUNTDOWN을 거친다. COUNTDOWN의 Escape·포커스 상실은 STAGE_SELECT로 복귀하고, PLAYING의 Escape·포커스 상실은 일시정지한다.

## D-012 — 사운드는 보조 정보이며 음소거 가능해야 한다

- 상태: **확정**
- 결정: 박자, 충돌, 회피, 피격에 짧은 효과음을 제공하되 모든 정보는 무음 상태에서도 읽을 수 있게 한다.
- 이유: 사운드는 리듬감을 크게 높이지만 브라우저 자동 재생 정책, 사용 환경, 청각 접근성을 고려해야 한다.
- 결과: TITLE의 `Space`, `Enter`, 클릭을 포함한 첫 사용자 상호작용 뒤 오디오를 활성화하고 항상 음소거 토글을 제공한다. D-038의 절차형 스코어도 이 접근성 원칙과 음소거 상태의 완전한 시각 정보 규칙을 따른다.

## D-013 — 웨이브 시간은 프레임이 아닌 단조 시계를 따른다

- 상태: **확정**
- 결정: 예고·충돌·해제와 웨이브 일정은 단조 시간 값을 기준으로 계산하고, 일시정지 구간을 논리 시간에서 제외한다.
- 이유: 낮은 프레임률, 백그라운드 탭, 렌더링 지연이 웨이브 수나 판정 순서를 바꾸지 않아야 한다.
- 결과: 프레임 사이에 충돌 시점을 지나쳤더라도 해당 이벤트를 정확히 한 번 처리한다.

## D-014 — 1차 목표는 데스크톱 브라우저다

- 상태: **확정**
- 결정: 물리 키보드가 있는 데스크톱 Chrome/Edge 계열 브라우저를 우선 지원한다.
- 이유: 핵심 조작이 실제 QWERTY 키 위치에 의존한다.
- 결과: 작은 화면은 가능한 범위에서 반응형으로 표시하지만 모바일 터치 대체 조작은 프로토타입 범위가 아니다.

## D-015 — 프로토타입은 로컬 단일 플레이로 유지한다

- 상태: **확정**
- 결정: 서버, 계정, 데이터베이스, 온라인 순위표를 추가하지 않는다. 반복 플레이 기록은 현재 브라우저의 `localStorage`에만 저장한다.
- 이유: 핵심 회피가 검증되기 전에 인프라가 학습 속도를 늦추지 않게 한다.
- 결과: 로컬 기록은 플레이를 막는 진행 게이트가 아니며 STAGE 01~06은 처음부터 모두 열린다. 클라우드 동기화나 기기 간 이전은 제공하지 않는다.
- 재검토 조건: 플레이테스트에서 타인과의 기록 비교가 핵심 유지 요인으로 확인될 때 계정·온라인 기록을 별도로 검토한다.

## D-016 — 현재 위치가 공격 대상이면 별도 경고 계층을 사용한다

- 상태: **확정**
- 결정: 플레이어 셀이 예고 대상에 포함되면 일반 위험 셀과 달리 빨간 외곽 링, `TARGET / MOVE!` 라벨, 화면 테두리, 상단 경고 문구와 전용 고음 큐를 동시에 표시한다.
- 이유: 기존 초록색 현재 위치 표시가 붉은 공격 예고를 덮어, 플레이어가 자신의 위험 여부를 즉시 구분하기 어려웠다.
- 결과: 색만 보지 않아도 `현재 위치 안전`과 `현재 위치 공격 대상`을 구분할 수 있고, 실제 피격 뒤에는 `HIT`과 남은 HP를 별도로 보여준다.

## D-017 — 처음부터 세 스테이지를 선택할 수 있고 활성 키가 단계적으로 늘어난다

- 상태: **확정**
- 현재 적용: **D-022와 D-024가 이 결정을 대체한다.** 아래 내용은 변경 전 이력으로 보존한다.
- 결정: STAGE_SELECT에서 잠금 없이 STAGE 01~03을 모두 선택할 수 있게 한다. STAGE 01은 홈로우 `ASDFGHJKL` 9키/16웨이브, STAGE 02는 여기에 `ERTYUI`와 `CVB`를 더한 18키/22웨이브, STAGE 03은 A~Z 26키/30웨이브를 사용한다.
- 이유: 초보자는 작은 공간에서 핵심 이동을 익히고, 숙련자는 기다림이나 해금 없이 전체 키보드 난이도에 바로 도전할 수 있어야 한다.
- 결과: 물리 `KeyQ`/`KeyW`/`KeyE`가 각각 STAGE 01/02/03 카드에 대응한다. 실제 확정은 D-020의 2단계 선택 규칙을 따른다. `KeyF` 시작은 모든 스테이지에서 유지하고, 활성 키 목록은 단일 스테이지 설정으로 관리한다.

## D-018 — STAGE 02~03에는 활성 키 중 하나만 안전한 공격을 넣는다

- 상태: **확정**
- 현재 적용: 대상 스테이지와 판정 기준은 **D-025가 대체한다.**
- 결정: STAGE 02와 STAGE 03의 고정 시퀀스에 활성 키 중 정확히 하나만 제외하고 나머지를 모두 공격하는 웨이브를 포함한다.
- 이유: 넓은 위험을 피하는 것에서 한 단계 나아가 키보드 위치를 빠르게 찾아가는 숙련을 시험하되, 정답이 명확한 패턴으로 유지하기 위함이다.
- 결과: 유일한 안전 키에는 색 외에 `ONLY SAFE` 라벨을 표시한다. 데이터 검증에서 공격 대상이 `활성 키 수 - 1`개인지와 안전 키가 활성 셀인지 확인한다.

## D-019 — STAGE 02~03에는 입력과 위치를 함께 요구하는 희귀 회복 웨이브를 넣는다

- 상태: **확정**
- 현재 적용: 대상 스테이지와 빈도는 **D-025가 대체한다.**
- 결정: STAGE 02와 STAGE 03에 약 23웨이브당 1회 수준의 회복 웨이브를 둔다. 예고 중 지정 키를 실제 입력하고 판정 순간에도 그 셀에 있으면 HP를 1 회복하며 최대 HP 5를 넘지 않는다.
- 이유: 회피만 이어지는 흐름에 매우 드문 긍정적 목표를 넣고, 위험을 감수해 정확히 키를 찾아간 플레이에 회복 기회를 주기 위함이다.
- 결과: 현재 고정 시퀀스에서는 STAGE 03~06에 각각 1회 배치한다. 회복 키는 파랑/초록과 `HEAL` 라벨을 함께 사용한다. 입력 또는 위치 조건을 놓쳐도 피해는 없고, 회복 웨이브는 콤보를 바꾸지 않는다.

## D-020 — 스테이지 선택은 무선택 상태에서 강조와 확정을 분리한다

- 상태: **확정**
- 현재 적용: 2단계 선택 모델은 유지하고 여섯 단축키 매핑은 **D-022로 확장한다.**
- 결정: STAGE_SELECT에 처음 진입할 때 강조된 카드를 두지 않는다. Q/W/E 또는 카드 클릭의 첫 입력은 대응 카드를 강조하고, 이미 강조된 같은 대상의 두 번째 입력이 스테이지를 확정한다. 화살표는 강조만 만들거나 이동하고, Enter는 강조된 카드가 있을 때만 확정한다.
- 이유: 선택 화면 진입과 동시에 임의의 카드가 선택된 것처럼 보이는 문제를 없애고, 실수로 키나 카드를 한 번 건드려 게임이 즉시 시작되는 것을 막기 위함이다.
- 결과: 다른 Q/W/E나 다른 카드를 입력하면 강조 대상만 바뀐다. 강조가 없는 상태의 Enter는 아무 동작도 하지 않으며, 모든 입력 방식이 “강조 후 확정”이라는 같은 모델을 사용한다.

## D-021 — 모든 플레이 진입은 선택한 스테이지 BPM의 3박 카운트다운을 거친다

- 상태: **확정**
- 현재 적용: 흐름과 오프닝 BPM은 유지하고, 표시·준비 이동 규칙은 **D-028로 갱신한다.**
- 결정: 스테이지 확정, RETRY, NEXT STAGE 뒤에는 즉시 PLAYING으로 전환하지 않는다. COUNTDOWN에서 `3`을 즉시 표시하고 스테이지 BPM의 한 박마다 `2`, `1`로 진행한 뒤 마지막 한 박 후 PLAYING을 시작한다.
- 이유: 플레이어에게 손 위치를 준비할 시간을 주고, 시작 전에 해당 스테이지의 박자 차이를 몸으로 인지하게 하기 위함이다.
- 결과: COUNTDOWN 중에는 오프닝 활성 키 준비 이동만 가능하고 공격, 점수, 웨이브, 플레이 타이머와 콤보는 진행되지 않는다. Escape 또는 포커스 상실은 예약된 시작을 취소하고 무선택 STAGE_SELECT로 안전하게 복귀한다.

## D-022 — 선택 가능한 스테이지를 여섯 개로 세분화한다

- 상태: **확정**
- 현재 적용: 여섯 스테이지와 키 매핑은 유지하고, 웨이브 수와 목표 길이는 **D-026으로 갱신한다.**
- 결정: 처음부터 STAGE 01~06을 모두 열고 물리 `KeyQ`/`KeyW`/`KeyE`/`KeyR`/`KeyT`/`KeyY`에 순서대로 대응한다.
- 이유: 세 단계만으로는 입문과 전체 키보드 숙련 사이의 난이도 도약이 커서, 난이도를 더 세밀하고 점진적으로 나눌 필요가 있다.
- 결과: 초기 무선택, 첫 입력 강조·같은 입력 재확정, 화살표 탐색과 Enter 확정 모델은 여섯 카드에 동일하게 적용한다. 카드의 공간 배치는 D-030의 가로 스크롤 덱을 따른다. NEXT STAGE는 STAGE 01~05 클리어 때만 제공하고 STAGE 06 클리어에서는 RETRY를 기본으로 한다.

## D-023 — 템포는 스테이지 안의 네 구간에서 변한다

- 상태: **확정**
- 결정: 하나의 고정 BPM을 폐기하고 각 스테이지를 `OPENING → EXPANSION/SPEED-UP → CONTRACTION/SLOWDOWN → LARGER/FASTER FINALE`로 구성한다. STAGE 01~06의 오프닝 BPM은 92/100/108/116/124/132, 전체 BPM 범위는 각각 82–110/88–122/94–132/102–142/108–150/116–156이다.
- 이유: 한 속도로 끝까지 진행하는 단조로움을 줄이고, 느려지는 긴장 완화와 다시 빨라지는 러시를 패턴 학습의 일부로 만들기 위함이다.
- 결과: 3→2→1, RETRY와 NEXT의 카운트다운은 해당 스테이지의 오프닝 BPM을 사용한다. 플레이 중 현재 BPM과 웨이브 간격은 템포 맵에서 결정적으로 계산하고, 재시도하면 같은 변화가 재현된다. 일시정지는 템포 구간과 다음 판정의 남은 시간까지 함께 동결한다.

## D-024 — 활성 키는 패턴별 존으로 확장·축소한다

- 상태: **확정**
- 현재 적용: 활성 키 범위와 F 포함 규칙은 유지하고, 전환 빈도·후보·예고 순서는 **D-027로 갱신한다.**
- 결정: 스테이지 전체에서 활성 키 수를 고정하지 않는다. STAGE 01~06의 활성 키 범위는 각각 5–11/8–15/10–19/12–22/15–25/18–26이며, 네 구간과 패턴 설계에 따라 존이 확장·축소된다. 모든 존은 `KeyF`를 포함한다.
- 이유: 스테이지가 시작되자마자 결정된 키 수만 반복하는 느낌을 없애고, 좁은 공간의 정밀 회피와 넓은 공간 탐색을 한 판 안에서 교차시키기 위함이다.
- 결과: 공격 대상, 이동 입력과 안전 셀 계산은 현재 런타임 존만 사용하며 모든 공격은 그 안에 최소 하나의 안전 셀을 남긴다. 축소는 갑작스러운 선행 F 재배치가 아니라 직전 위험 키의 붕괴 경고와 플레이어의 직접 이탈을 우선한다.

## D-025 — 특수 패턴과 HUD는 현재 템포·존을 기준으로 한다

- 상태: **확정**
- 결정: `ONLY SAFE`와 회복 웨이브는 STAGE 03~06에 포함하고 회복은 해당 스테이지마다 1회만 둔다. 모든 특수 패턴은 스테이지 최대 키 목록이 아니라 현재 존을 기준으로 생성한다. HUD에는 현재 BPM, 템포 섹션, 활성 키 수와 존 변경 예고를 항상 표시한다.
- 이유: 템포와 키 영역이 바뀌는 상태에서 정적인 스테이지 수치만 보여주면 현재 규칙과 다음 행동을 알기 어렵다.
- 결과: 선택 카드는 오프닝/범위 BPM과 활성 키 범위를 비교 정보로 제공한다. 플레이 중에는 `SLOWDOWN`, `ACCEL`, `RUSH`, `COLLAPSE WARNING`, `RESTORE WARNING`을 색 외 텍스트·형태 단서로 전달한다.

## D-026 — 각 스테이지를 2분에 가까운 고정 시퀀스로 확장한다

- 상태: **확정**
- 결정: STAGE 01~06의 웨이브 수를 각각 52/56/60/64/68/72로 확장한다. 현재 BPM, 공격 간격, 공격 판정 잔상과 위험 기반 존 전환 예고를 합산한 논리 길이는 각각 약 115.2/114.9/113.7/113.4/114.5/114.0초다.
- 이유: 기존의 짧은 웨이브 구성은 스테이지의 감속·가속과 존 변화가 충분히 전개되기 전에 끝났다. 한 판 안에서 패턴을 읽고 적응한 뒤 피날레를 경험할 시간을 확보하기 위함이다.
- 결과: 각 네 템포 섹션은 STAGE 01부터 13웨이브, 이후 14/15/16/17/18웨이브 단위로 균등하게 전개된다. 선택 카드, HUD, 결과와 테스트는 새 전체 웨이브 수를 단일 스테이지 설정에서 읽는다.

## D-027 — 존 변화는 직전 공격의 여파를 전용 경고로 전달한다

- 상태: **확정**
- 결정: 활성 존 목표 크기는 기본적으로 5웨이브 펄스마다 변한다. 축소할 때는 직전 공격의 위험 대상이었던 활성 키 중 `KeyF`를 제외한 셀만 붕괴 후보로 삼고, `COLLAPSE WARNING` 1.5박 뒤 비활성화한다. 확장할 때는 새 셀을 `RESTORE WARNING` 1.25박 동안 보여준 뒤 활성화한다. 적용 후 결과 표기는 각각 `FIELD COLLAPSED`, `ZONE RESTORED`다.
- 이유: 키가 이유 없이 갑자기 사라지거나 나타나는 느낌을 없애고, “방금 위험했던 구역이 무너진다”는 공격과 공간 변화의 인과를 만들기 위함이다.
- 결과: 붕괴 후보가 목표 감소량보다 적어도 안전 셀을 임의로 추가 제거하지 않는다. 전환 경고 중에는 다음 공격이 진행되지 않으며, 붕괴 키는 경고가 끝날 때까지 이동 가능하므로 플레이어가 직접 탈출할 수 있다. 적용 순간에도 남은 플레이어의 피해와 재배치는 D-029를 따른다. 일시정지하면 경고의 남은 시간도 함께 동결한다.

## D-028 — 카운트다운은 보드를 가리지 않는 준비 단계다

- 상태: **확정**
- 결정: 오프닝 BPM의 3→2→1은 전면 오버레이가 아니라 키보드 위의 작은 준비 배지로 표시한다. COUNTDOWN 중 오프닝 활성 키 이동을 허용한다.
- 이유: 카운트다운 동안 실제 키 배열과 시작 위치를 보며 손과 플레이어 위치를 준비할 수 있게 하기 위함이다.
- 결과: 준비 이동만 기록하고 공격, 점수, 콤보, 웨이브, 플레이 타이머와 무적 시간은 PLAYING 전까지 정지한다. Escape 또는 포커스 상실의 취소 규칙과 RETRY/NEXT의 카운트다운 경로는 유지한다.

## D-029 — 붕괴 셀 잔류는 피해 후 최근접 활성 키로 밀어낸다

- 상태: **확정**
- 결정: `COLLAPSE WARNING` 종료 순간 제거되는 셀에 플레이어가 남아 있으면 피해 1을 판정한다. 일반 공격과 같은 500ms 무적 타이머를 공유하므로 이미 무적이면 HP와 무적 종료 시각을 바꾸지 않는다. 피해 적용 여부와 관계없이 플레이어는 새 존에서 물리적으로 가장 가까운 활성 키로 즉시 이동한다.
- 거리 규칙: 화면의 공통 QWERTY 키 중심 좌표를 사용한 제곱 유클리드 거리로 비교한다. 거리가 같으면 공통 `ALL_KEYS`의 행 우선·왼쪽 우선 순서에서 먼저 나오는 키를 선택한다.
- 이유: 충분한 붕괴 경고를 무시한 선택에는 명확한 대가를 주면서도, 사라진 셀에 플레이어가 남는 불가능 상태를 예측 가능한 방식으로 해소하기 위함이다.
- 결과: 고정 `KeyF` 폴백은 폐기한다. 붕괴 피격은 콤보를 초기화하고 일반 피격 피드백을 사용하며, HP가 0이면 다음 공격을 예약하지 않고 GAME_OVER로 전환한다.

## D-030 — 스테이지 선택은 왼쪽부터 이어지는 가로 카드 덱이다

- 상태: **확정**
- 결정: STAGE 01~06 카드를 왼쪽→오른쪽 순서의 가로 스크롤·스냅 덱으로 배치한다. Q/W/E/R/T/Y, 화살표 또는 첫 클릭으로 강조된 카드는 덱 중앙으로 스크롤되고 주변 카드보다 확대·상승해 전면으로 튀어나온다.
- 이유: 여섯 개를 한 화면의 고정 격자로 나열하는 메뉴보다, 키 입력으로 스테이지를 찾아가고 선택 대상이 물리적으로 튀어나오는 감각을 주기 위함이다.
- 결과: 초기 무선택과 첫 입력 강조·두 번째 입력 확정 규칙은 유지한다. 스크롤과 팝업 모션은 `prefers-reduced-motion`에서 즉시 전환으로 대체하고, 카드 버튼의 포커스·접근성 이름은 그대로 제공한다.

## D-031 — 여섯 인터페이스 테마는 시각만 바꾼다

- 상태: **확정**
- 결정: 제공 테마를 **POLAR WHITE**(기본), **WARM WHITE**, **MONO SIGNAL**, **SIGNAL BLUE**, **AMBER OPS**, **IVORY LAB**의 정확히 여섯 개로 고정한다. TITLE, STAGE_SELECT와 플레이 화면에서 설정을 열 수 있으며 COUNTDOWN 중에는 설정 진입을 비활성화한다.
- 이유: 동일한 게임을 밝은 연구실형, 고대비 신호형과 어두운 작전형 등 서로 다른 시각 취향으로 즐길 수 있게 하되, 시작 박자나 회피 판정을 테마 조작이 방해하지 않게 하기 위함이다.
- 결과: PLAYING 중 설정을 열면 먼저 기존 PAUSED 상태로 전환하고 닫은 뒤 사용자가 명시적으로 재개한다. 테마 전환은 시각 토큰과 브라우저 색상 모드만 바꾸며 타이머, 입력, 공격, BPM, 활성 존, HP, 점수, 콤보와 게임 상태를 변경하거나 초기화하지 않는다. 각 선택 버튼은 적용 전에도 그 테마의 배경·형태·타이포·QWERT 키캡·5색 팔레트를 미니 프리뷰로 보여준다.

## D-032 — 영구 측면 콤보 통계 대신 상단 계기판과 일시적 연속 회피 피드백을 사용한다

- 상태: **확정**
- 결정: 플레이 중 핵심 상태는 화면 상단의 가로 계기판형 HUD에 스테이지, 웨이브, 점수, HP, 현재 BPM, 템포 섹션, 활성 키 수와 남은 진행도로 묶는다. 성공적인 연속 회피는 키보드 바깥 상단 HUD의 우측 진행도 칸에 잠시 나타나는 `PERFECT DODGE / n COMBO!` 피드백으로 전달하고 영구 측면 콤보 통계는 두지 않는다. 피드백은 키 캡과 공격 신호를 가리지 않으며 애니메이션 뒤 DOM에서도 제거한다.
- 이유: 키보드와 공격 신호에 시선을 집중하면서도 현재 런의 핵심 수치를 한 줄에서 빠르게 훑고, 콤보는 성취 순간에만 강하게 느끼게 하기 위함이다.
- 결과: 최고 콤보는 결과 화면에 남아 재도전 비교에 사용한다. D-025의 현재 BPM·섹션·활성 존·전환 경고 정보와 D-030의 가로 스크롤·팝업 스테이지 덱은 그대로 유지한다.

## D-033 — TITLE은 시작 경험만 남기고 기술 수치를 제거한다

- 상태: **확정**
- 결정: TITLE에는 로고, 한 줄 경험 설명, 시작 버튼과 물리 키보드 권장 안내만 둔다. 스테이지 수, 키 수, BPM 범위 같은 기술 수치는 표시하지 않는다.
- 이유: 첫 화면을 사양표가 아니라 게임의 입구처럼 읽히게 하고 시작 행동의 우선순위를 분명히 하기 위함이다.
- 결과: 스테이지별 웨이브·키·BPM 정보는 STAGE_SELECT 카드에, 현재 값은 플레이 HUD에 남긴다. 기존 TITLE의 `06 STAGES`, 키 수와 BPM 수치 블록은 제거한다.

## D-034 — 로컬 최고 기록과 런 등급을 잠금 없이 제공한다

- 상태: **확정**
- 결정: 각 스테이지에 시도 수, 클리어 수, 최고 도달 웨이브, 최고 점수, 최고 콤보, 클리어 최고 잔여 HP, 최고 등급·레이팅을 로컬 저장한다. 레이팅은 완주도 45%, 점수 30%, 최고 콤보 15%, 잔여 HP 10%로 계산하고 `S ≥ 95`, `A ≥ 85`, `B ≥ 70`, `C ≥ 50`, 나머지는 `D`로 구분한다. 게임 오버 런은 최대 79점이다.
- 이유: 스테이지 잠금이나 계정 시스템 없이도 한 판의 성과를 읽고 같은 콘텐츠에 다시 도전할 명확한 목표를 만들기 위함이다.
- 결과: 모든 스테이지는 계속 열려 있고 STAGE_SELECT 카드에서 해당 기록을 요약한다. CLEARED/GAME_OVER 보고서는 등급·레이팅·점수·최고 콤보·잔여 HP·웨이브와 첫 클리어/최고 등급/최고 점수/최고 콤보/최고 HP/최장 도달 배지를 표시한다.

## D-035 — 런 종료는 중앙 경로에서 정확히 한 번 커밋한다

- 상태: **확정**
- 결정: 일반 공격 피격, 붕괴 피격과 마지막 웨이브 클리어를 포함한 모든 종료 경로는 하나의 런 완료 함수로 모은다. 런마다 고유 ID를 부여하고 최근 ID를 보존해 같은 결과의 중복 커밋을 무시한다.
- 이유: 비동기 상태 갱신이나 React 개발 모드 재실행 때문에 최종 점수 누락, 시도·클리어 중복 증가 또는 서로 다른 결과 화면이 생기지 않게 하기 위함이다.
- 결과: 종료 순간의 점수, 최고 콤보, HP와 도달 웨이브를 같은 스냅샷으로 평가·저장한 뒤 결과 보고서를 만든다. RETRY, NEXT와 STAGE SELECT는 이미 끝난 런을 다시 집계하지 않는다.

## D-036 — 네 구간을 ACT 전환과 스테이지 식별색으로 묶는다

- 상태: **확정**
- 결정: 두 번째 템포 섹션부터 시작 경계에 `ACT n / 4`, 섹션명과 템포 방향을 표시하고 마지막 구간은 `FINAL SEQUENCE`로 강조한다. 플레이 진행선과 주요 강조는 선택한 스테이지의 고유 식별색을 사용한다.
- 이유: 약 2분의 시퀀스를 연속 웨이브가 아니라 시작·확장·수축·피날레의 4막으로 체감하게 하고 스테이지별 개성을 강화하기 위함이다.
- 결과: ACT 큐는 키보드와 공격 라벨을 가리지 않는 진행 영역에 나타나며 색 외에도 ACT 번호, 섹션명과 상태 문구를 제공한다. 마지막 ACT는 다른 전환보다 강하지만 짧고 절제된 모션을 사용한다.

## D-037 — WAVE SURGE는 결정적인 3웨이브 방향 패턴이다

- 상태: **확정**
- 결정: 0부터 세는 `waveIndex 5`부터 13웨이브마다 3연속 일반 공격을 `WAVE SURGE`로 구성한다. 시퀀스 방향은 왼쪽→오른쪽과 오른쪽→왼쪽을 번갈아 사용하고, 각 웨이브의 공격 대상은 당시 런타임 활성 존과 공통 QWERTY 좌표에서 다시 계산해 최소 2개의 안전 키를 남긴다.
- 이유: 한 번의 돌발 공격보다 읽을 수 있는 공간 문장을 만들어 다음 파도 방향을 예측하고 선제 이동하는 재미를 주면서, 존 확장·축소와 결합해도 같은 재시도 결과와 공정성을 보존하기 위함이다.
- 결과: 진행 영역에는 방향 화살표, 3연속 중 현재 순서와 현재 위치의 이동 필요 여부를 표시하고 키보드에도 방향성 파도 신호를 제공한다. 자동 테스트는 시작·주기·방향 교대·런타임 존 재계산·최소 2개 안전 키와 전체 빈도를 고정한다.

## D-038 — 외부 음원 없이 원본 절차형 Web Audio 스코어를 합성한다

- 상태: **확정**
- 결정: 제3자 녹음, 샘플 또는 기성 작곡을 사용하지 않고 브라우저 Web Audio로 스코어 전체를 런타임 합성한다. STAGE 01~06은 서로 다른 모티프를 가지며 4/4의 킥·펄스·하이햇·베이스·모티프 레이어가 현재 웨이브 프로필의 ACT, BPM과 강도를 따른다.
- 이유: 외부 오디오 자산과 라이선스 의존 없이 여섯 스테이지의 정체성과 약 2분 런의 완급을 강화하고, 동적 템포와 ACT 변화를 음악에서도 즉시 들려주기 위함이다.
- 결과: 순수 마디 계획은 결정적으로 생성되고 실제 음은 절대 `AudioContext` 시각에 미리 예약한다. 공격 간격은 0.25박 단위로 양자화하고, 판정 잔상 뒤 시작하는 존 전환에서는 게임이 음악 위상을 단방향으로 다시 맞춘다. 화면·오디오 카운트인은 같은 24ms 시작 여유를 사용한다. 게임의 `performance.now()` 타임라인이 공격·충돌·상태 전환의 권위 있는 기준으로 남으며 음악은 이를 따라가기만 한다. 카운트다운·시작·일시정지·재개·음소거·결과·스테이지 이탈·해제 수명주기와 비동기 작업 무효화를 연결했다. 자동 테스트는 마디 계획, 여섯 모티프, ACT·강도 변주, 절대 예약, 카운트인, 취소 경합과 Web Audio 미지원 폴백을 검증하며 실제 수명주기와 청음은 수동 검증 대상으로 남긴다.

## 보류된 결정

다음은 핵심 루프 플레이테스트 전에는 확정하지 않는다.

- 최종 타이틀과 로고
- 외부 녹음·샘플·기성 작곡을 사용하는 음원 제작과 완전 고정 곡별 스테이지 구조
- 점수 보상 수치와 런 등급 가중치의 플레이테스트 튜닝
- 로컬 기록 삭제·내보내기 UX
- 특수키 능력과 보스 패턴
- 모바일 또는 게임패드 대체 조작
