# DECISIONS.md — Keyboard Dodge

이 문서는 이미 선택한 설계의 이유와 향후 재검토 조건을 남긴다. 상태는 `확정`, `가설`, `보류`로 구분한다.

## D-001 — 플레이 공간은 알파벳 26키로 제한한다

- 상태: **확정**
- 결정: 표준 영문 QWERTY의 A~Z만 플레이 셀로 사용한다.
- 이유: 실제 키보드 공간 기억이라는 핵심 아이디어를 가장 익숙하고 단순한 범위에서 검증할 수 있다.
- 결과: 숫자, 기능키, 방향키, Space 등은 프로토타입 범위에서 제외한다.

## D-002 — 물리 키 위치는 `KeyboardEvent.code`로 읽는다

- 상태: **확정**
- 결정: 입력 판정은 `KeyboardEvent.key`가 아니라 `KeyboardEvent.code`를 사용한다.
- 이유: 한글 IME와 입력 언어가 바뀌어도 동일한 물리 QWERTY 위치를 유지해야 한다.
- 결과: 유효 코드는 `KeyA`~`KeyZ`이며 반복 `keydown`은 무시한다.

## D-003 — 마지막 유효 입력이 현재 위치다

- 상태: **확정**
- 결정: 플레이어는 `KeyF`에서 시작하고, 마지막으로 누른 유효 알파벳 키로 즉시 이동한다.
- 이유: 이동 거리나 물리 시뮬레이션이 아니라 키보드 전체를 순간 탐색하는 감각을 직접 검증하기 위함이다.
- 결과: 키 홀드와 인접 이동 제한, 이동 시간, 경로 충돌은 없다.

## D-004 — 충돌은 Key Cell 포함 여부로 판정한다

- 상태: **확정**
- 결정: 충돌 순간 공격 대상 키 집합에 현재 키가 포함되는지만 검사한다.
- 이유: 화면 애니메이션과 판정을 분리해 결과를 명확하고 테스트 가능하게 유지한다.
- 결과: 픽셀 Hitbox, 캐릭터 크기, 이동 중간 경로는 프로토타입에서 고려하지 않는다.

## D-005 — HP는 5, 피격 무적은 500ms다

- 상태: **확정**
- 결정: 최대 HP 5, 피격당 피해 1, 피해 적용 뒤 500ms 무적을 사용한다.
- 이유: 초보자에게 여러 번의 학습 기회를 주면서 겹친 패턴이 한 번에 HP를 소진시키는 것을 막는다.
- 재검토 조건: 플레이테스트에서 한 판이 지나치게 관대하거나 중첩 공격의 결과가 이해되지 않을 때 수치를 조정한다.

## D-006 — 한 판의 목표는 45초 생존이다

- 상태: **확정**
- 결정: HP가 남은 채 45초에 도달하면 클리어하고, HP가 0이면 즉시 게임 오버다.
- 이유: 핵심 루프를 충분히 경험하면서도 재시도 부담이 작은 세션 길이다.
- 결과: 점수나 콤보는 승리 조건이 아니다. 마지막 충돌과 45초가 겹치면 충돌 판정을 먼저 적용한다.

## D-007 — 리듬 구조와 생존 판정을 분리한다

- 상태: **확정**
- 결정: 공격 예고와 충돌은 박자에 놓되, 생존은 충돌 순간 안전 셀에 있는지만 판정한다.
- 이유: 리듬 입력 정확도와 공간 회피를 동시에 강제하면 실패 원인이 모호해진다. 박자는 공격 예측과 손맛을 돕는 역할에 집중한다.
- 결과: 이른 이동이나 늦은 이동에 별도 HP 페널티를 주지 않는다. 타이밍 등급을 추가하더라도 생존 결과와 분리한다.

## D-008 — 기본 템포는 120 BPM으로 시작한다

- 상태: **가설**
- 결정: 프로토타입 기본값은 120 BPM(한 박 500ms)으로 두고 설정 한 곳에서 조정한다.
- 이유: 예고, 500ms 무적, 박자 펄스의 관계를 이해하기 쉬운 출발점이다.
- 재검토 조건: 초보자가 1박 예고를 읽지 못하거나, 숙련 구간이 둔하게 느껴질 때 BPM과 예고 박 수를 함께 튜닝한다.

## D-009 — 난이도는 경과 시간으로만 상승한다

- 상태: **확정**
- 결정: 0~15초, 15~30초, 30~45초의 세 구간을 기준으로 패턴 복잡도와 연속성을 높인다.
- 이유: 매 판의 학습 곡선을 예측 가능하게 만들고 성과가 좋은 플레이어를 즉시 처벌하지 않기 위함이다.
- 결과: 콤보나 현재 HP가 공격 난이도를 동적으로 올리지 않는다. 세부 셀 수와 패턴 가중치는 조정 가능하다.

## D-010 — 콤보는 연속 회피 피드백이다

- 상태: **확정**
- 결정: 피해 없이 공격을 통과하면 콤보 +1, 실제 피격 시 0으로 초기화한다.
- 이유: 생존이라는 단순한 목표 안에서도 숙련 향상과 재도전 성과를 보이기 위함이다.
- 결과: 무적 중 위험 셀에 남은 것은 회피 성공으로 세지 않는다. 최고 콤보는 결과 화면에 표시한다.

## D-011 — 게임 상태와 일시정지를 명시적으로 관리한다

- 상태: **확정**
- 결정: `READY`, `PLAYING`, `PAUSED`, `CLEARED`, `GAME_OVER` 상태를 분리한다.
- 이유: 입력, 타이머, 공격, 결과 오버레이가 동시에 진행되는 버그를 막는다.
- 결과: Escape/버튼으로 일시정지하며 문서가 숨겨지거나 창 포커스를 잃으면 자동 일시정지한다. 재개는 사용자가 명시적으로 수행한다.

## D-012 — 사운드는 보조 정보이며 음소거 가능해야 한다

- 상태: **확정**
- 결정: 박자, 충돌, 회피, 피격에 짧은 효과음을 제공하되 모든 정보는 무음 상태에서도 읽을 수 있게 한다.
- 이유: 사운드는 리듬감을 크게 높이지만 브라우저 자동 재생 정책, 사용 환경, 청각 접근성을 고려해야 한다.
- 결과: 첫 사용자 상호작용 뒤 오디오를 활성화하고 항상 음소거 토글을 제공한다.

## D-013 — 게임 시간은 프레임이 아닌 단조 시계를 따른다

- 상태: **확정**
- 결정: 45초 타이머와 공격 일정은 단조 시간 값을 기준으로 계산하고, 일시정지 구간을 논리 시간에서 제외한다.
- 이유: 낮은 프레임률, 백그라운드 탭, 렌더링 지연이 공격 횟수나 클리어 시간을 바꾸지 않아야 한다.
- 결과: 프레임 사이에 충돌 시점을 지나쳤더라도 해당 이벤트를 정확히 한 번 처리한다.

## D-014 — 1차 목표는 데스크톱 브라우저다

- 상태: **확정**
- 결정: 물리 키보드가 있는 데스크톱 Chrome/Edge 계열 브라우저를 우선 지원한다.
- 이유: 핵심 조작이 실제 QWERTY 키 위치에 의존한다.
- 결과: 작은 화면은 가능한 범위에서 반응형으로 표시하지만 모바일 터치 대체 조작은 프로토타입 범위가 아니다.

## D-015 — 프로토타입은 로컬 단일 플레이로 유지한다

- 상태: **확정**
- 결정: 서버, 계정, 데이터베이스, 온라인 순위표를 추가하지 않는다.
- 이유: 핵심 회피가 검증되기 전에 인프라가 학습 속도를 늦추지 않게 한다.
- 재검토 조건: 플레이테스트에서 반복 플레이 의사가 확인되고 기록 비교가 핵심 유지 요인으로 나타날 때 로컬 기록부터 검토한다.

## 보류된 결정

다음은 핵심 루프 플레이테스트 전에는 확정하지 않는다.

- 최종 타이틀과 로고
- 배경 음악 및 곡별 스테이지 구조
- 점수 공식과 콤보 배율
- 영구 최고 기록 저장 방식
- 난이도 선택 모드
- 특수키 능력과 보스 패턴
- 모바일 또는 게임패드 대체 조작
